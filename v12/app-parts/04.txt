状态",$('#connectionPill span').textContent,"自动降级"]].map(x=>`<article class="summary-card glass"><span>${x[0]}</span><b>${x[1]}</b><small>${x[2]}</small></article>`).join('');$('#marketTableBody').innerHTML=list.map(p=>{const m=currentMarket(p.base);return`<tr><td><div class="asset-cell"><span class="mini-coin" style="background:${p.color};color:#fff">${p.icon}</span><span><b>${p.base}/USDT</b><small>${p.name}</small></span></div></td><td>${fmt(m.last,p.decimals)}</td><td class="${m.change>=0?'positive':'negative'}">${pct(m.change)}</td><td>${fmt(m.high,p.decimals)}</td><td>${fmt(m.low,p.decimals)}</td><td>${compact(m.volumeQuote)}</td><td><button class="market-action" data-trade-pair="${p.base}">交易</button></td></tr>`}).join('');$$('[data-trade-pair]').forEach(b=>b.onclick=()=>{selectPair(b.dataset.tradePair);navigate('trade')})}
function drawEquity(){const c=$('#equityCanvas'),r=c.getBoundingClientRect(),d=Math.min(2,devicePixelRatio||1);if(!r.width)return;c.width=r.width*d;c.height=r.height*d;const x=c.getContext('2d');x.scale(d,d);const vals=state.account.equityHistory.map(v=>v.v);while(vals.length<20)vals.unshift(state.account.initial);const min=Math.min(...vals),max=Math.max(...vals),range=max-min||1;x.clearRect(0,0,r.width,r.height);const pts=vals.map((v,i)=>({x:6+(r.width-12)*i/(vals.length-1),y:8+(r.height-16)*(1-(v-min)/range)}));const grad=x.createLinearGradient(0,0,r.width,0);grad.addColorStop(0,'#5f78f6');grad.addColorStop(1,'#13b887');x.beginPath();pts.forEach((p,i)=>i?x.lineTo(p.x,p.y):x.moveTo(p.x,p.y));x.strokeStyle=grad;x.lineWidth=2;x.stroke();x.lineTo(r.width-6,r.height);x.lineTo(6,r.height);x.closePath();const fill=x.createLinearGradient(0,0,0,r.height);fill.addColorStop(0,'rgba(95,120,246,.2)');fill.addColorStop(1,'rgba(95,120,246,0)');x.fillStyle=fill;x.fill()}
function drawAllocation(items){const c=$('#allocationCanvas'),r=c.getBoundingClientRect(),d=Math.min(2,devicePixelRatio||1);if(!r.width)return;c.width=r.width*d;c.height=r.height*d;const x=c.getContext('2d');x.scale(d,d);x.clearRect(0,0,r.width,r.height);const colors=['#5f78f6','#13b887','#ec5a70','#e7a92f','#8a71dc'],total=items.reduce((a,i)=>a+i.value,0)||1,cx=r.width/2,cy=r.height/2,rad=Math.min(r.width,r.height)*.38;let a=-Math.PI/2;items.forEach((it,i)=>{const n=a+it.value/total*Math.PI*2;x.beginPath();x.arc(cx,cy,rad,a,n);x.arc(cx,cy,rad*.62,n,a,true);x.closePath();x.fillStyle=colors[i%colors.length];x.fill();a=n});x.fillStyle=getComputedStyle(document.body).getPropertyValue('--text');x.textAlign='center';x.font='700 13px -apple-system';x.fillText(`${items.length} 项`,cx,cy+4);$('#allocationLegend').innerHTML=items.map((it,i)=>`<div><i style="background:${colors[i%colors.length]}"></i><span>${it.name}</span><b>${(it.value/total*100).toFixed(1)}%</b></div>`).join('')}
function renderAssets(){if(!$('#assetEquity'))return;const s=accountSnapshot();$('#assetEquity').textContent=money(s.equity);$('#assetReturn').textContent=pct(s.ret);$('#assetReturn').className=s.ret>=0?'positive':'negative';$('#assetCash').textContent=money(s.cash);$('#assetPositionValue').textContent=money(s.positionValue);$('#assetUnrealized').textContent=money(s.unreal);$('#assetUnrealized').className=s.unreal>=0?'positive':'negative';const items=[{name:'USDT',value:Math.max(0,s.cash)},...Object.values(state.account.positions).map(p=>({name:p.base,value:p.product==='spot'?Math.abs(p.qty)*currentMarket(p.base).last:p.margin+((currentMarket(p.base).last-p.avg)*p.qty)}))].filter(x=>x.value>0);requestAnimationFrame(()=>{drawEquity();drawAllocation(items)});const rows=Object.values(state.account.positions);$('#assetTableBody').innerHTML=rows.length?rows.map(pos=>{const p=PAIRS.find(x=>x.base===pos.base),px=currentMarket(pos.base).last,pnl=(px-pos.avg)*pos.qty,value=pos.product==='spot'?px*pos.qty:pos.margin+pnl;return`<tr><td>${p.base}</td><td>${pos.product==='spot'?'现货':'永续'}</td><td>${fmt(Math.abs(pos.qty),6)}</td><td>${fmt(pos.avg,p.decimals)}</td><td>${fmt(px,p.decimals)}</td><td>${money(value)}</td><td class="${pnl>=0?'positive':'negative'}">${pnl>=0?'+':''}${money(pnl)}</td></tr>`}).join(''):'<tr class="empty-row"><td colspan="7">当前资产全部为 USDT，暂无持仓。</td></tr>'}
function renderAnalytics(){if(!$('#riskScore'))return;const m=accountMetrics();$('#riskScore').textContent=m.risk;$('#riskLabel').textContent=m.risk<30?'低风险':m.risk<65?'中等风险':'高风险';$('#riskDescription').textContent=m.risk<30?'当前账户以现金为主，整体风险较低。':m.risk<65?'当前存在一定仓位敞口，建议控制单笔风险。':'当前仓位或杠杆敞口偏高，建议降低风险。';const col=m.risk<30?'var(--green)':m.risk<65?'#e7a92f':'var(--red)';$('#riskGauge').style.background=`radial-gradient(circle,var(--surface-solid) 58%,transparent 60%),conic-gradient(${col} 0 ${m.risk}%,rgba(127,139,156,.12) ${m.risk}% 100%)`;$('#performanceMetrics').innerHTML=[["累计收益",pct(m.ret),m.ret>=0?'positive':'negative'],["最大回撤",`${m.drawdown.toFixed(2)}%`,'negative'],["胜率",m.winRate==null?'--':`${m.winRate.toFixed(1)}%`,''],["盈亏比",m.profitFactor==null?'--':m.profitFactor.toFixed(2),''],["成交次数",state.account.trades.length,''],["累计手续费",money(state.account.fees),'']].map(x=>`<article class="metric-card glass"><span>${x[0]}</span><b class="${x[2]}">${x[1]}</b><small>基于本地模拟记录</small></article>`).join('');const risks=[["仓位集中度",Object.keys(state.account.positions).length>2?'分散':'集中度可控'],["永续敞口",Object.values(state.account.positions).some(x=>x.product==='perp')?'存在杠杆仓位':'无杠杆仓位'],["可用资金",`${(m.cash/Math.max(1,m.equity)*100).toFixed(1)}%`],["未实现盈亏",money(m.unreal)]];$('#riskList').innerHTML=risks.map(x=>`<div class="analysis-item"><span>${x[0]}<small>实时账户诊断</small></span><b>${x[1]}</b></div>`).join('');const discipline=[["单笔风险",'建议不超过权益1%'],["连续亏损",m.losses.length>=3?'需要暂停复盘':'当前正常'],["委托数量",`${state.account.orders.length} 笔`],["交易频率",state.account.trades.length>30?'偏高':'正常']];$('#disciplineLis