# ATLAS X V13 验收记录

测试日期：2026-07-10

## 核心结构
- V13 为独立工程，不加载 V7—V12 的页面或业务模块。
- 首屏介绍由本地 HTML/CSS 直接呈现，不等待行情接口。
- 图表与应用源码采用同源分段加载；所有 Git Blob 均完成哈希比对。
- 解码后的 `chart.js` 与 `app.js` 已分别通过 Node.js 语法检查。

## 响应式实测
- 390 × 844：通过
- 430 × 932：通过
- 1440 × 900：通过

## 交互实测
- 首屏进入终端：通过
- 现货／合约切换：通过
- 交易对选择：通过
- K线周期切换：通过
- K线轻点十字光标与单根数据：通过
- K线拖动、滚轮缩放、双指缩放：通过
- 图表全屏与退出：通过
- 订单类型、保证金模式、杠杆选择：通过
- 模拟下单、持仓、委托、成交：通过
- 行情、资产、分析页面切换：通过
- 抽屉关闭按钮：通过
- 点击遮罩关闭：通过
- 浏览器返回关闭抽屉：通过
- Escape 关闭抽屉／图表状态：通过
- 横向溢出：未发现
- 控制台运行错误：未发现

## 数据降级
- 优先读取 OKX 公共行情与K线接口。
- 接口失败时保留本地种子行情、K线和订单簿，页面仍可展示及模拟操作。

## 产品边界
- 实时公开行情与本地模拟交易原型。
- 不接入真实资金、真实订单、充值提现或生产级撮合系统。
