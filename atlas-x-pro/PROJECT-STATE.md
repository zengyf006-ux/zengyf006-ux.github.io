# ATLAS X Pro — 项目持续状态与交接记录

> 本文件是项目唯一长期交接依据。任何新聊天、执行工具或开发人员必须先读取本文件，再继续开发、验收或部署；不得依赖聊天记忆猜测。

最后更新：2026-07-11

## 1. 项目身份

- 仓库：`zengyf006-ux/zengyf006-ux.github.io`
- 产品目录：`atlas-x-pro/`
- 生产分支：`main`
- 主开发分支：`atlas-x-pro-terminal`
- 主开发 PR：`#4 ATLAS X Pro：专业交易终端系统级重构`，保持 Draft
- 阶段一 PR：`#5 ATLAS X Stage 1：实时行情与专业 K 线重构`，已合并
- 生产地址：`https://zengyf006-ux.github.io/atlas-x-pro/`
- 页面标题：`ATLAS X Pro｜专业模拟交易终端`

## 2. 产品边界

ATLAS X Pro 是公开行情驱动的高保真模拟交易终端：

- 不接真实资金、充值或提现。
- 不连接真实交易所账户。
- 不收集或保存 API Key、Secret、私钥或助记词。
- 订单、持仓、成交、策略、审计和预警仍为模拟账本或浏览器本地状态。
- 不得把随机演示数据标为实时，不得对外宣称已接入真实交易账户。

## 3. 当前生产状态

### 已部署版本

- 当前生产与开发分支共同 HEAD：`b5fe6fc2ccf99a065184fb6e391e34040201630f`
- 最新运行时代码提交：`61e4ca7e396c55502bf683ce3a6b72a7ae63c952`
- `b5fe6fc2...` 在运行时代码之后仅增强确定性 QA，不改变产品业务逻辑。
- 部署方式：`main` 从 `416a603da58d1dafc230264e54dfd401976b269d` 非强制快进至 `b5fe6fc2...`。
- 部署后比较：`main` 与 `atlas-x-pro-terminal` 为 `identical`。
- 本轮生产回滚分支：`rollback/atlas-x-pro-before-stage1-hardening-20260711-1312`
- 旧生产回滚分支：`rollback/atlas-x-pro-before-stage1-prod-20260711`
- 主开发回滚分支：`rollback/atlas-x-pro-terminal-before-stage1-20260711`
- GitHub Pages 目标路径仍为 `/atlas-x-pro/`；Pages 缓存或传播延迟不得通过修改运行时代码绕过。

### 最终发布门禁

精确候选 `b5fe6fc2ccf99a065184fb6e391e34040201630f`：

- `ATLAS X Pro acceptance`：Run `29153652776`，结论 `success`
  - Gateway contract：通过
  - 两个已部署公共网关 real-network smoke：通过
  - iPhone 390 × 844：全部适用步骤通过
  - iPhone 430 × 932：全部适用步骤通过
  - Desktop 1440 × 900：全部适用步骤通过
  - Desktop 1920 × 1080：全部适用步骤通过
- `ATLAS X Next visual QA`：Run `29153652775`，结论 `success`
- 无控制台错误、页面错误、白屏或横向溢出。
- 人工复核了四端主屏、下单面板、行情健康、预警中心、订单审计、工作区命令中心和追踪止损线。
- 手机和桌面没有新增关键遮挡、乱码、不可见控件或风险线丢失。

运行时代码 HEAD 变化后必须重新跑真实网关、四端完整验收、视觉 QA 和人工截图复核。只有 Markdown 交接记录变化可沿用对应运行时代码证据。

## 4. 已部署公共行情基础设施

### 统一实时行情网关

- Supabase 项目：`vtcunypvhtudragsittb`
- Function：`atlas-market-gateway`
- 部署 ID：`92975cba-f2d1-4b68-bd4c-66c602839102`
- 状态：`ACTIVE`
- 地址：`https://vtcunypvhtudragsittb.supabase.co/functions/v1/atlas-market-gateway`
- 接口：`/health`、`/markets`、`/snapshot`、`/candles`、`/stream`
- 上游：Binance、OKX、Bybit 公共行情

### 批量市场指标网关

- Function：`atlas-market-gateway-markets`
- 部署 ID：`b51ec9a3-e983-4071-88a8-ab5f50701b70`
- 状态：`ACTIVE`
- 地址：`https://vtcunypvhtudragsittb.supabase.co/functions/v1/atlas-market-gateway-markets`
- 用途：统一返回 12 个市场的价格、涨跌、成交额、振幅、买一、卖一、点差和成交笔数

### 安全边界

- 只处理公开市场数据。
- 不接收账户、订单、余额、密钥、个人信息或支付数据。
- 使用 Origin 白名单、GET/OPTIONS 限制、参数白名单、超时、限流和标准化错误。
- 禁止任意 URL 代理，避免 SSRF。

## 5. 阶段一已完成能力

### 实时行情与缓存

- `market-data-engine` 统一管理交易对、周期、快照、K 线、订单簿、逐笔成交和实时连接。
- IndexedDB 缓存优先首屏，不再每次空白等待远端响应。
- 切交易对或周期时递增请求代际，取消旧请求并关闭旧连接。
- 旧回包不能覆盖当前周期。
- 连接状态区分 `booting`、`live`、`reconnecting`、`stale`、`offline`。
- 数据过期后退出“实时”状态，不伪造随机价格。

### K 线周期与交互

支持并验证：

- `1m`、`3m`、`5m`、`15m`、`30m`
- `1h`、`2h`、`4h`、`6h`、`12h`
- `1d`、`1w`

K 线详情包含时间、周期、开盘、最高、最低、收盘、涨跌额、涨跌幅、振幅、成交量、成交额、EMA10、EMA20、数据源和收盘状态。

交互包含：

- 同一根再次点击取消。
- 关闭按钮、Esc、空白区域、切周期、切交易对和重置均可取消。
- 显示可视区最高价、最低价、最新价线和当前蜡烛倒计时。
- 切周期时保留旧图作非阻塞过渡，不再长时间白屏。

### 专业市场筛选器

- 统一使用公共网关，不维护第二套浏览器直连行情源。
- 支持搜索、成交额、涨跌幅、振幅、点差和价格排序。
- 支持高振幅、低点差、自选同步和最多四市场比较。
- 筛选与比较偏好持久化。
- 网关失败时只使用有效缓存；缓存过期后缺失指标显示 `--`。

### 稳定性与启动链加固

- 数据健康中心已迁移到统一行情会话。
- 修复数据健康面板动态挂载后未及时写入 provider、周期和路由状态的竞态。
- 所有质量层 CSS 完成加载后才初始化交易、预警、审计、风险和工作区模块。
- 启动器明确区分 `booting`、`ready` 和 `failed`，不再在样式未生效时宣布完成。
- 修复预警表单被实时重绘清空、规则创建竞态、标签页闪回和手机触控尺寸。
- 修复手机审计、持仓卡片、市场入口和追踪止损标记的异步几何竞态。
- 修复市场筛选器搜索在实时重绘后失效。
- 预警和追踪止损专项 QA 只读取当前可见节点，并等待真实样式与几何稳定后判定；未降低门槛。

## 6. 当前完整能力

- 公开实时行情、缓存首屏和明确的新鲜度状态。
- 市场列表、自选、搜索、筛选和市场比较。
- 12 个 K 线周期、订单簿、深度图和逐笔成交。
- 市价、限价和高级模拟委托。
- 余额、持仓、委托、成交、费用、滑点和盈亏。
- 下单执行保护、大额确认、交易安全锁和二次解锁。
- 账本派生绩效、组合风险、风险仓位计算和交易计划。
- 图表交易层、OCO、追踪止损、分批退出和统一预留协调。
- 工作区模式、命令中心、预设和快捷键。
- 专业预警中心。
- 订单与成交审计中心。
- 数据健康、中文语义字体和高级视觉状态。

## 7. 人工视觉复核与待改进项

### 本轮通过

- 390、430 手机主屏保持独立移动交易流程，没有桌面压缩式布局。
- 下单底部面板字段完整，买卖入口、订单类型、数量、金额、计划、高级委托和退出策略可读。
- 行情健康面板、预警中心和订单审计在两个手机尺寸上无横向溢出。
- 追踪止损线及“高于/低于可视区”风险语义正常显示。
- 1440 与 1920 桌面四区工作台稳定，K 线、订单簿、逐笔成交、票据和账户区对齐正常。
- 命令中心继续使用抽屉覆盖层，没有挤压桌面核心交易区。

### 阶段二必须解决

- 手机顶部状态、周期和图表工具区仍偏密。
- 手机蜡烛详情卡遮挡图表面积偏大。
- 手机图表、盘口、下单和持仓仍像多个模块拼接，缺少成熟交易所的一体化节奏。
- 订单簿列宽、精度聚合和买卖盘模式仍需强化。
- 下单面板的订单类型、数量/金额切换、费用、滑点和成交均价预估仍需系统重排。
- 桌面低优先级文字偏小，后续通过信息密度档位解决，不做粗暴全局放大。

## 8. 不可回退的工程规则

- 不使用 iframe 套旧版。
- 不把随机演示价格标为实时。
- 不在 `main` 直接试错。
- 不强制覆盖开发或生产分支。
- 不通过删除断言、跳过测试或放宽核心规则制造绿灯。
- 新功能优先进入抽屉、详情页或按需区域，不无节制挤压桌面主屏。
- 手机必须作为独立产品流程设计，不把桌面布局简单压窄。
- 每轮完成后更新本文件并保留明确回滚点。

## 9. 下一阶段唯一优先任务

**阶段二：移动交易主屏、订单簿与下单流程系统级重排。**

目标：

1. 按“市场状态 → 周期/工具 → K 线 → 盘口/成交 → 下单 → 持仓/审计”重建手机信息层级。
2. 将 K 线详情改为更轻、可折叠、少遮挡的移动交互。
3. 增加手机全屏图表和底部指标/绘图工具面板。
4. 订单簿价格、数量和累计数量使用稳定列宽与等宽数字。
5. 支持精度聚合、双边、仅买盘、仅卖盘切换。
6. 下单面板完善市价、限价、止损限价、止损市价、数量/金额切换、仓位比例、手续费、滑点和成交均价预估。
7. 统一买卖按钮、风险提示、审计入口和退出策略入口。
8. 新增手机专项门禁并继续跑四种视口完整回归。

阶段二通过前，不进入模拟永续合约账本，也不宣称网站是最终成品。

## 10. 更新日志

### 2026-07-11：阶段一硬化补丁生产部署

- 运行时代码：`61e4ca7e396c55502bf683ce3a6b72a7ae63c952`
- 最终验收 HEAD：`b5fe6fc2ccf99a065184fb6e391e34040201630f`
- 完整验收：Run `29153652776`，四端与双网关全部通过
- 视觉 QA：Run `29153652775`，通过
- 部署方式：非强制快进 `main`
- 回滚分支：`rollback/atlas-x-pro-before-stage1-hardening-20260711-1312`
- 修复：动态行情健康面板同步、CSS 先于模块初始化、预警可见节点验收、追踪风险线稳定验收

### 2026-07-11：阶段一生产部署

- 生产运行时代码：`8d4b5b839e89072913148572cb0c3dedf085ec54`
- 完整验收：Run `29151110208`，四端和双网关全部通过
- 视觉 QA：Run `29151110197`，通过
- 部署方式：非强制快进 `main`
- 生产回滚分支：`rollback/atlas-x-pro-before-stage1-prod-20260711`

### 2026-07-11：订单与成交审计中心部署

- 上线运行时代码：`8276f1cdae1f8198b094ad903c085df7a8f4ec9c`
- 部署记录提交：`1d7dceaea3755e11e690319c95c5d600ffd5809a`
- 回滚分支：`rollback/atlas-x-pro-before-audit-20260711`
