(()=>{'use strict';const $=id=>document.getElementById(id);const canvas=$('game'),ctx=canvas.getContext('2d',{alpha:false,desynchronized:true});const ui={score:$('score'),combo:$('combo'),stage:$('stage'),phaseName:$('phaseName'),phaseFill:$('phaseFill'),guideTitle:$('guideTitle'),guideText:$('guideText'),voiceBtn:$('voiceBtn'),focusBtn:$('focusBtn'),overloadBtn:$('overloadBtn'),toast:$('toast'),voiceBubble:$('voiceBubble'),voiceText:$('voiceText'),overlay:$('overlay'),startBtn:$('startBtn'),quietBtn:$('quietBtn')};const TAU=Math.PI*2;let W=0,H=0,DPR=1;let running=false,last=0,time=0,phaseTime=0,phaseIndex=0,stage=1,score=0,combo=1,chain=0,charge=0;let soundOn=true,voiceOn=true,audio=null,voiceCooldown=0,focusCooldown=0,focusUntil=0,screenFlash=0,shake=0;let pointer={down:false,x:0,y:0,px:0,py:0,holdStart:0};let targets=[],particles=[],rings=[],trails=[],stars=[];let resonance={active:false,progress:0,window:0.13,score:0,miss:0};let boss=null;const phases=[{name:'裂解阶段',duration:26,title:'点击目标，拖动可连续切开。',text:'棱晶会分裂，链核会引爆附近目标。'},{name:'共振阶段',duration:22,title:'当脉冲环贴合核心时点击。',text:'时机越准，音效与分数反馈越强。'},{name:'高压核心',duration:34,title:'观察发光弱点，连续击穿 Boss。',text:'弱点会轮换，错误位置只会消耗节奏。'}];const palette={cyan:'#7be8ff',violet:'#9a7cff',amber:'#ffc96b',rose:'#ff6fa8',green:'#71efbd',white:'#f7f8fc'};function resize(){W=innerWidth;H=innerHeight;DPR=Math.min(devicePixelRatio||1,1.5);canvas.width=Math.floor(W*DPR);canvas.height=Math.floor(H*DPR);canvas.style.width=W+'px';canvas.style.height=H+'px';ctx.setTransform(DPR,0,0,DPR,0,0);if(!stars.length){for(let i=0;i<46;i++)stars.push({x:Math.random(),y:Math.random(),z:Math.random()});}}addEventListener('resize',resize,{passive:true});resize();function clamp(v,a,b){return Math.max(a,Math.min(b,v))}function rand(a,b){return Math.random()*(b-a)+a}function distance(ax,ay,bx,by){return Math.hypot(ax-bx,ay-by)}function segmentDistance(px,py,ax,ay,bx,by){const dx=bx-ax,dy=by-ay,l=dx*dx+dy*dy||1;let t=((px-ax)*dx+(py-ay)*dy)/l;t=clamp(t,0,1);return Math.hypot(px-(ax+t*dx),py-(ay+t*dy))}function formatStage(n){return String(n).padStart(2,'0')}function initAudio(){if(!soundOn)return;const AC=window.AudioContext||window.webkitAudioContext;if(!AC)return;if(!audio)audio=new AC();if(audio.state==='suspended')audio.resume();}function tone(freq,dur=.08,type='sine',gain=.025,slide=0,delay=0){if(!soundOn||!audio)return;const t=audio.currentTime+delay,o=audio.createOscillator(),g=audio.createGain();o.type=type;o.frequency.setValueAtTime(freq,t);if(slide)o.frequency.exponentialRampToValueAtTime(Math.max(45,freq+slide),t+dur);g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(gain,t+.008);g.gain.exponentialRampToValueAtTime(.0001,t+dur);o.connect(g).connect(audio.destination);o.start(t);o.stop(t+dur+.02);}function noise(dur=.06,gain=.015,cut=1600){if(!soundOn||!audio)return;const len=Math.floor(audio.sampleRate*dur),buf=audio.createBuffer(1,len,audio.sampleRate),data=buf.getChannelData(0);for(let i=0;i<len;i++)data[i]=(Math.random()*2-1)*(1-i/len);cons