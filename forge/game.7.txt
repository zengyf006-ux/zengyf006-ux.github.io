ings[i];r.life-=dt*1.5;r.r+=dt*95;if(r.life<=0){rings.splice(i,1);continue}ctx.globalAlpha=r.life;ctx.strokeStyle=r.color;ctx.lineWidth=r.width;ctx.beginPath();ctx.arc(r.x,r.y,r.r,0,TAU);ctx.stroke()}ctx.globalAlpha=1;for(let i=trails.length-1;i>=0;i--){const t=trails[i];t.life-=dt*3.6;if(t.life<=0){trails.splice(i,1);continue}ctx.globalAlpha=t.life;ctx.strokeStyle=palette.cyan;ctx.lineWidth=2+6*t.life;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(t.ax,t.ay);ctx.lineTo(t.bx,t.by);ctx.stroke()}ctx.globalAlpha=1;}function updatePhase(dt){if(phaseIndex===2&&boss===null&&phaseTime<phases[2].duration-1)return;phaseTime+=dt;if(phaseTime>=phases[phaseIndex].duration)nextPhase();if(phaseIndex===0){for(const t of targets){const slow=focusUntil>time?.42:1;t.rot+=t.spin*dt*slow;t.pulse+=dt;t.life+=dt;t.x+=t.vx*dt*slow;t.y+=t.vy*dt*slow;if(t.x<t.r+18||t.x>W-t.r-18)t.vx*=-1;if(t.y<122+t.r||t.y>H-150-t.r)t.vy*=-1}for(let i=targets.length-1;i>=0;i--)if(targets[i].dead)targets.splice(i,1);const desired=Math.min(13,6+Math.floor(stage*.55));while(targets.length<desired)spawnTarget();}}function drawChargeArc(){const cx=W/2,cy=H*.47,r=Math.min(W,H)*.25;ctx.strokeStyle='rgba(255,255,255,.055)';ctx.lineWidth=2;ctx.beginPath();ctx.arc(cx,cy,r,-Math.PI*.86,-Math.PI*.14);ctx.stroke();ctx.strokeStyle=charge>=100?palette.amber:palette.cyan;ctx.lineWidth=3;ctx.beginPath();ctx.arc(cx,cy,r,-Math.PI*.86,-Math.PI*.86+Math.PI*.72*charge/100);ctx.stroke();}function frame(ts){if(!running)return;if(!last)last=ts;let dt=Math.min(.033,(ts-last)/1000);last=ts;time+=dt;if(voiceCooldown>0)voiceCooldown-=dt;if(focusCooldown>0)focusCooldown-=dt;updatePhase(dt);const sx=shake>0?rand(-shake,shake):0,sy=shake>0?rand(-shake,shake):0;shake=Math.max(0,shake-dt*22);ctx.save();ctx.translate(sx,sy);drawBackground();drawChargeArc();if(phaseIndex===0)targets.forEach(drawTarget);else if(phaseIndex===1)drawResonance();else drawBoss();updateAndDrawEffects(dt);ctx.restore();if(screenFlash>0){screenFlash=Math.max(0,screenFlash-dt*2.8);ctx.fillStyle='rgba(255,255,255,'+screenFlash*.5+')';ctx.fillRect(0,0,W,H)}updateHUD();requestAnimationFrame(frame);}ui.startBtn.addEventListener('click',()=>start(false));ui.quietBtn.addEventListener('click',()=>start(true));function start(quiet){soundOn=!quiet;voiceOn=!quiet;initAudio();ui.overlay.classList.add('hidden');running=true;resetGame();if(soundOn){sfx('perfect');setTimeout(()=>speak('锋哥，锻炉已经启动。别想别的，先把第一层打穿。','start'),300)}requestAnimationFrame(frame)}ui.voiceBtn.addEventListener('click',()=>{voiceOn=!voiceOn;if(!voiceOn&&'speechSynthesis'in window)speechSynthesis.cancel();toast(voiceOn?'语音已开启':'语音已关闭');updateHUD()});ui.focusBtn.addEventListener('click',()=>{if(running)useFocus()});ui.overloadBtn.addEventListener('click',()=>{if(running)useOverload()});document.addEventListener('visibilitychange',()=>{if(document.hidden){last=0;if(audio?.state==='running')audio.suspend()}else if(running){last=0;if(soundOn)audio?.resume()}});if('speechSynthesis'in window)speechSynthesis.getVoices();})();