ow.addColorStop(0,'rgba(123,232,255,.3)');glow.addColorStop(.35,'rgba(154,124,255,.08)');glow.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=glow;ctx.beginPath();ctx.arc(cx,cy,105,0,TAU);ctx.fill();ctx.strokeStyle='rgba(255,255,255,.17)';ctx.lineWidth=2;ctx.beginPath();ctx.arc(cx,cy,78,0,TAU);ctx.stroke();ctx.strokeStyle=perfect?palette.amber:palette.cyan;ctx.lineWidth=perfect?5:3;ctx.globalAlpha=.85;ctx.beginPath();ctx.arc(cx,cy,ringR,0,TAU);ctx.stroke();ctx.globalAlpha=1;ctx.fillStyle='rgba(7,9,14,.92)';ctx.strokeStyle=palette.violet;ctx.lineWidth=2;ctx.beginPath();ctx.arc(cx,cy,37,0,TAU);ctx.fill();ctx.stroke();ctx.fillStyle=perfect?palette.amber:palette.white;ctx.font='800 11px -apple-system,BlinkMacSystemFont,"PingFang SC"';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(perfect?'NOW':'SYNC',cx,cy);}function drawBoss(){if(!boss)return;boss.rot+=.0035*(focusUntil>time?.45:1);boss.weakTimer+=1/60;if(boss.weakTimer>3.1){boss.weak=(boss.weak+1+Math.floor(Math.random()*4))%6;boss.weakTimer=0}const x=boss.x,y=boss.y,r=boss.r;const glow=ctx.createRadialGradient(x,y,10,x,y,r*1.65);glow.addColorStop(0,'rgba(255,111,168,.25)');glow.addColorStop(.52,'rgba(154,124,255,.08)');glow.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=glow;ctx.beginPath();ctx.arc(x,y,r*1.65,0,TAU);ctx.fill();ctx.save();ctx.translate(x,y);ctx.rotate(boss.rot);for(let i=0;i<6;i++){const a=i*TAU/6;ctx.save();ctx.rotate(a);const active=i===boss.weak;ctx.strokeStyle=active?palette.amber:'rgba(255,255,255,.18)';ctx.fillStyle=active?'rgba(255,201,107,.15)':'rgba(255,255,255,.025)';ctx.lineWidth=active?4:2;ctx.beginPath();ctx.arc(0,0,r,-TAU/12,TAU/12);ctx.lineTo(Math.cos(TAU/12)*r*.67,Math.sin(TAU/12)*r*.67);ctx.arc(0,0,r*.67,TAU/12,-TAU/12,true);ctx.closePath();ctx.fill();ctx.stroke();if(active){ctx.fillStyle=palette.amber;ctx.beginPath();ctx.arc(r*.84,0,5+Math.sin(time*6)*2,0,TAU);ctx.fill()}ctx.restore()}ctx.restore();ctx.fillStyle='rgba(6,8,13,.94)';ctx.strokeStyle='rgba(255,255,255,.2)';ctx.lineWidth=2;ctx.beginPath();ctx.arc(x,y,r*.52,0,TAU);ctx.fill();ctx.stroke();const hp=boss.hp/boss.maxHp;ctx.strokeStyle='rgba(255,255,255,.1)';ctx.lineWidth=7;ctx.beginPath();ctx.arc(x,y,r*.39,0,TAU);ctx.stroke();ctx.strokeStyle=hp>.35?palette.rose:palette.amber;ctx.beginPath();ctx.arc(x,y,r*.39,-Math.PI/2,-Math.PI/2+TAU*hp);ctx.stroke();ctx.fillStyle=palette.white;ctx.font='800 12px -apple-system,BlinkMacSystemFont,"PingFang SC"';ctx.textAlign='center';ctx.fillText('HIGH PRESSURE',x,y-4);ctx.fillStyle='rgba(255,255,255,.48)';ctx.font='600 10px -apple-system,BlinkMacSystemFont,"PingFang SC"';ctx.fillText(boss.hp+' / '+boss.maxHp,x,y+14);}function updateAndDrawEffects(dt){for(let i=particles.length-1;i>=0;i--){const p=particles[i];p.x+=p.vx;p.y+=p.vy;p.vx*=.985;p.vy=p.vy*.985+.025;p.life-=dt*1.75;if(p.life<=0){particles.splice(i,1);continue}ctx.globalAlpha=p.life;ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(p.x,p.y,p.size*p.life,0,TAU);ctx.fill()}ctx.globalAlpha=1;for(let i=rings.length-1;i>=0;i--){const r=r