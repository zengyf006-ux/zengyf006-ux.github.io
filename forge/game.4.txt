ity==='miss'){resonance.miss++;chain=0;combo=1;sfx('miss');addRing(cx,cy,palette.rose,48,2);toast('偏离');}else{resonance.score+=gain;score+=gain*combo;chain++;combo=1+Math.floor(chain/4);charge=clamp(charge+gain*1.4,0,100);sfx(quality==='perfect'?'perfect':'hit',quality==='perfect'?2:1);vibrate(quality==='perfect'?[10,18,10]:8);addParticles(cx,cy,quality==='perfect'?palette.amber:palette.cyan,quality==='perfect'?28:16,quality==='perfect'?1.6:1.1);addRing(cx,cy,quality==='perfect'?palette.amber:palette.cyan,62,quality==='perfect'?4:2);toast(quality==='perfect'?'完美共振':quality==='good'?'精准':'命中');if(resonance.score>=28&&resonance.score-gain<28)speak('共振稳定。你已经把节奏控制住了。','resonance');}}function hitBoss(x,y){if(!boss)return;const a=(Math.atan2(y-boss.y,x-boss.x)-boss.rot+TAU)%TAU;const seg=Math.floor(a/(TAU/6));if(seg!==boss.weak){sfx('miss');addRing(x,y,palette.rose,10,2);shake=Math.min(6,shake+1);return;}boss.hp--;boss.flash=1;score+=3*combo;chain++;combo=1+Math.floor(chain/4);charge=clamp(charge+5+combo,0,100);sfx('boss');vibrate([12,14,12]);addParticles(x,y,palette.amber,24,1.5);addRing(x,y,palette.amber,18,3);shake=Math.min(12,shake+3);if(boss.hp%4===0){boss.weak=(boss.weak+1+Math.floor(Math.random()*4))%6;boss.weakTimer=0;toast('弱点转移');}if(boss.hp<=0){score+=80+stage*10;charge=100;screenFlash=.7;addParticles(boss.x,boss.y,palette.white,70,2.4);addRing(boss.x,boss.y,palette.amber,boss.r*.6,5);sfx('overload');vibrate([20,35,22]);speak('高压核心已经击穿。漂亮，这一轮你赢了。','boss');boss=null;setTimeout(nextPhase,1200);}}function useFocus(){if(focusCooldown>0){toast('聚焦正在恢复');return}focusCooldown=11;focusUntil=time+3.5;toast('聚焦 3 秒');speak('聚焦模式开启。只看当前目标。','focus');sfx('perfect');}function useOverload(){if(charge<100){toast('超载尚未充满');return}charge=0;screenFlash=.85;shake=14;sfx('overload');vibrate([18,30,18,30,24]);if(phaseIndex===0){targets.forEach(t=>{if(!t.dead){t.dead=true;addParticles(t.x,t.y,t.color,16,1.8)}});score+=targets.length*2*combo;targets.length=0;for(let i=0;i<8;i++)spawnTarget(true)}if(phaseIndex===1){resonance.score+=18;score+=40*combo;addParticles(W/2,H*.47,palette.amber,70,2.2);}if(phaseIndex===2&&boss){boss.hp=Math.max(0,boss.hp-5);addParticles(boss.x,boss.y,palette.amber,70,2.2);if(boss.hp===0)hitBoss(boss.x+boss.r,boss.y)}speak('超载释放。把这一屏的杂音全部清空。','overload');}function pointerPosition(e){const r=canvas.getBoundingClientRect();return{x:e.clientX-r.left,y:e.clientY-r.top}}function onDown(e){if(!running)return;e.preventDefault();initAudio();const p=pointerPosition(e);pointer.down=true;pointer.x=pointer.px=p.x;pointer.y=pointer.py=p.y;pointer.holdStart=time;if(phaseIndex===1){tapResonance();return}if(phaseIndex===2){hitBoss(p.x,p.y);return}for(let i=targets.length-1;i>=0;i--){const t=targets[i];if(!t.dead&&distance(p.x,p.y,t.x,t.y)<t.r+14){hitTarget(t);break}}}function onMove(e){if(!running||!pointer.down)return;e.preventDefault();const p=pointerPosition(e);addTrail(pointer.x,pointer.y,p.x,p.y);pointer.px=pointer.x;pointer.py=pointer.y;pointer.x=p.x;pointer.y=p.y;if(phaseInd