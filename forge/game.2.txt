t src=audio.createBufferSource(),filter=audio.createBiquadFilter(),g=audio.createGain(),t=audio.currentTime;src.buffer=buf;filter.type='lowpass';filter.frequency.value=cut;g.gain.setValueAtTime(gain,t);g.gain.exponentialRampToValueAtTime(.0001,t+dur);src.connect(filter).connect(g).connect(audio.destination);src.start(t);src.stop(t+dur+.02);}function sfx(kind,quality=1){if(kind==='hit'){tone(240+quality*70,.055,'triangle',.024,-90);tone(580+quality*120,.045,'sine',.018,-160,.012);noise(.045,.011,2200)}if(kind==='perfect'){tone(420,.07,'triangle',.03,230);tone(720,.1,'sine',.025,200,.05);tone(1080,.12,'sine',.018,-180,.11)}if(kind==='chain'){tone(300,.07,'sawtooth',.022,330);tone(640,.08,'triangle',.025,180,.035);noise(.08,.014,1900)}if(kind==='boss'){tone(92,.22,'square',.045,-30);tone(48,.28,'sine',.05,-10);noise(.16,.022,760)}if(kind==='overload'){tone(75,.36,'sawtooth',.045,250);tone(210,.3,'triangle',.035,590,.08);tone(760,.24,'sine',.025,-220,.16);noise(.22,.026,1100)}if(kind==='miss'){tone(150,.09,'square',.012,-60)}}function vibrate(p){if(navigator.vibrate)navigator.vibrate(p)}function speak(text,key='generic'){showVoice(text);if(!voiceOn||!('speechSynthesis'in window)||voiceCooldown>0)return;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='zh-CN';u.rate=.94;u.pitch=.88;u.volume=.88;const voices=speechSynthesis.getVoices();const zh=voices.find(v=>/^zh/i.test(v.lang));if(zh)u.voice=zh;speechSynthesis.speak(u);voiceCooldown=key==='boss'?4.5:2.8;}function showVoice(text){ui.voiceText.textContent=text;ui.voiceBubble.classList.add('show');clearTimeout(showVoice.t);showVoice.t=setTimeout(()=>ui.voiceBubble.classList.remove('show'),2400)}function toast(text){ui.toast.textContent=text;ui.toast.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>ui.toast.classList.remove('show'),900)}function resetGame(){targets.length=particles.length=rings.length=trails.length=0;boss=null;resonance={active:false,progress:0,window:.13,score:0,miss:0};phaseIndex=0;phaseTime=0;stage=1;score=0;combo=1;chain=0;charge=0;focusCooldown=0;focusUntil=0;screenFlash=0;shake=0;time=0;last=0;setPhase(0,true);updateHUD();}function setPhase(i,initial=false){phaseIndex=i;phaseTime=0;targets.length=0;rings.length=0;resonance.active=i===1;boss=null;const p=phases[i];ui.phaseName.textContent=p.name;ui.guideTitle.textContent=p.title;ui.guideText.textContent=p.text;if(i===0)spawnInitialTargets();if(i===1){resonance.progress=0;resonance.score=0;resonance.miss=0;}if(i===2)spawnBoss();if(!initial){stage++;ui.stage.textContent=formatStage(stage);toast(p.name);speak(i===1?'进入共振阶段。别追速度，先抓住节奏。':i===2?'高压核心出现。观察亮起的弱点，逐层击穿它。':'裂解阶段重新开启。把节奏接回来。',i===2?'boss':'stage');}}function nextPhase(){setPhase((phaseIndex+1)%phases.length)}function updateHUD(){ui.score.textContent=score;ui.combo.textContent='×'+combo;ui.stage.textContent=formatStage(stage);ui.phaseFill.style.width=clamp(phaseTime/phases[phaseIndex].duration*100,0,100)+'%';ui.overloadBtn.classList.toggle('ready',charge>=100);