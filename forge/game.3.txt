ui.overloadBtn.classList.toggle('locked',charge<100);ui.focusBtn.classList.toggle('locked',focusCooldown>0);ui.voiceBtn.querySelector('.label').textContent=voiceOn?'语音开':'语音关';}function spawnInitialTargets(){for(let i=0;i<7;i++)spawnTarget(true)}function spawnTarget(initial=false,parent=null){if(targets.length>18)return;const roll=Math.random();let type='shard';if(roll<.13)type='chain';else if(roll<.27)type='prism';else if(roll<.36)type='anchor';const r=type==='anchor'?28:rand(17,24);const safeTop=120,safeBottom=140;const x=parent?parent.x+rand(-42,42):rand(36,W-36);const y=parent?parent.y+rand(-38,38):rand(safeTop,H-safeBottom);const hue=type==='chain'?palette.amber:type==='prism'?palette.violet:type==='anchor'?palette.rose:palette.cyan;targets.push({type,x:clamp(x,36,W-36),y:clamp(y,safeTop,H-safeBottom),r,hp:type==='anchor'?2:1,rot:rand(0,TAU),spin:rand(-1.1,1.1),pulse:rand(0,TAU),color:hue,life:initial?rand(0,2):0,vx:rand(-18,18),vy:rand(-15,15),dead:false});}function spawnBoss(){boss={x:W/2,y:H*.47,r:Math.min(128,Math.max(90,W*.24)),hp:18+Math.min(12,stage*2),maxHp:18+Math.min(12,stage*2),rot:0,weak:0,weakTimer:0,flash:0};rings.push({x:boss.x,y:boss.y,r:boss.r*.65,life:1,color:palette.rose,width:3});}function addParticles(x,y,color,count=12,speed=1){const room=180-particles.length;count=Math.max(0,Math.min(count,room));for(let i=0;i<count;i++){const a=rand(0,TAU),s=rand(1.8,6.2)*speed;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:1,size:rand(1.5,4.8),color});}}function addRing(x,y,color,r=12,width=2){if(rings.length>24)rings.shift();rings.push({x,y,r,life:1,color,width})}function addTrail(ax,ay,bx,by){if(trails.length>12)trails.shift();trails.push({ax,ay,bx,by,life:1})}function hitTarget(t,bonus=1){if(t.dead)return;t.hp--;t.pulse=0;t.rot+=.28;sfx('hit',bonus);vibrate(combo>=5?[8,12,8]:7);addParticles(t.x,t.y,t.color,10+Math.min(combo,8),1+combo*.02);addRing(t.x,t.y,t.color,t.r*.5,2);shake=Math.min(7,shake+1.5);if(t.hp>0){t.r*=1.08;return;}t.dead=true;score+=Math.round((t.type==='anchor'?4:t.type==='chain'?3:t.type==='prism'?2:1)*combo);chain++;combo=1+Math.floor(chain/5);charge=clamp(charge+4+combo*.8,0,100);if(t.type==='prism'){for(let i=0;i<3;i++)targets.push({type:'shard',x:t.x+rand(-30,30),y:t.y+rand(-30,30),r:12,hp:1,rot:rand(0,TAU),spin:rand(-1.7,1.7),pulse:rand(0,TAU),color:palette.green,life:0,vx:rand(-28,28),vy:rand(-24,24),dead:false});}if(t.type==='chain'){sfx('chain');const nearby=targets.filter(o=>!o.dead&&o!==t&&distance(o.x,o.y,t.x,t.y)<145).slice(0,5);nearby.forEach((o,i)=>setTimeout(()=>hitTarget(o,1.2),i*55));}if(chain===10)speak('十次连击。节奏已经抓住了。','combo10');if(chain===25)speak('二十五次连击。很好，现在完全进入状态了。','combo25');if(charge>=100&&charge-(4+combo*.8)<100)speak('超载已经充满。你随时可以清空全场。','ready');}function tapResonance(){const p=resonance.progress%1;const dist=Math.abs(p-.5);let quality='miss';let gain=0;if(dist<.055){quality='perfect';gain=8;}else if(dist<.12){quality='good';gain=4;}else if(dist<.2){quality='ok';gain=2;}const cx=W/2,cy=H*.47;if(qual