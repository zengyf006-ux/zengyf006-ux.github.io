ex!==0)return;for(let i=targets.length-1;i>=0;i--){const t=targets[i];if(!t.dead&&segmentDistance(t.x,t.y,pointer.px,pointer.py,p.x,p.y)<t.r+6)hitTarget(t,1.15)}}function onUp(){pointer.down=false}canvas.addEventListener('pointerdown',onDown,{passive:false});canvas.addEventListener('pointermove',onMove,{passive:false});addEventListener('pointerup',onUp,{passive:true});addEventListener('pointercancel',onUp,{passive:true});function drawBackground(){const g=ctx.createLinearGradient(0,0,0,H);g.addColorStop(0,'#080b14');g.addColorStop(.48,'#05070d');g.addColorStop(1,'#030407');ctx.fillStyle=g;ctx.fillRect(0,0,W,H);const rg=ctx.createRadialGradient(W*.5,H*.45,20,W*.5,H*.45,Math.max(W,H)*.7);rg.addColorStop(0,'rgba(62,85,150,.12)');rg.addColorStop(.45,'rgba(49,39,100,.055)');rg.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=rg;ctx.fillRect(0,0,W,H);ctx.globalAlpha=.22;for(const s of stars){const x=s.x*W,y=(s.y*H+time*(5+8*s.z))%H;ctx.fillStyle=s.z>.7?'#9cecff':'#ffffff';ctx.fillRect(x,y,1+s.z,1+s.z)}ctx.globalAlpha=1;ctx.strokeStyle='rgba(116,151,255,.035)';ctx.lineWidth=1;const step=44;ctx.beginPath();for(let x=(time*4)%step;x<W;x+=step){ctx.moveTo(x,0);ctx.lineTo(x,H)}for(let y=(time*7)%step;y<H;y+=step){ctx.moveTo(0,y);ctx.lineTo(W,y)}ctx.stroke();}function drawHex(x,y,r,rot,color,alpha=1){ctx.save();ctx.translate(x,y);ctx.rotate(rot);ctx.beginPath();for(let i=0;i<6;i++){const a=i*TAU/6;const px=Math.cos(a)*r,py=Math.sin(a)*r;i?ctx.lineTo(px,py):ctx.moveTo(px,py)}ctx.closePath();ctx.globalAlpha=alpha;ctx.strokeStyle=color;ctx.lineWidth=1.5;ctx.stroke();ctx.globalAlpha=1;ctx.restore()}function drawTarget(t){const pulse=1+Math.sin(time*2.4+t.pulse)*.06;ctx.save();ctx.translate(t.x,t.y);ctx.rotate(t.rot);ctx.globalAlpha=t.dead?0:1;const glow=ctx.createRadialGradient(0,0,1,0,0,t.r*1.8);glow.addColorStop(0,t.color+'70');glow.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=glow;ctx.beginPath();ctx.arc(0,0,t.r*1.8,0,TAU);ctx.fill();ctx.strokeStyle=t.color;ctx.fillStyle='rgba(8,11,17,.75)';ctx.lineWidth=t.type==='anchor'?2.5:1.7;if(t.type==='shard'){ctx.beginPath();for(let i=0;i<5;i++){const a=i*TAU/5-.5;const rr=t.r*(i%2?.72:1);const x=Math.cos(a)*rr*pulse,y=Math.sin(a)*rr*pulse;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.closePath();ctx.fill();ctx.stroke()}else if(t.type==='prism'){drawHex(0,0,t.r*pulse,0,t.color);drawHex(0,0,t.r*.58,-t.rot*1.7,t.color,.7)}else if(t.type==='chain'){ctx.beginPath();ctx.arc(0,0,t.r*pulse,0,TAU);ctx.fill();ctx.stroke();for(let i=0;i<3;i++){ctx.rotate(TAU/3);ctx.beginPath();ctx.moveTo(t.r*.3,0);ctx.lineTo(t.r*1.35,0);ctx.stroke()}}else{drawHex(0,0,t.r*pulse,0,t.color);ctx.beginPath();ctx.arc(0,0,t.r*.54,0,TAU);ctx.stroke();}ctx.restore();}function drawResonance(){const cx=W/2,cy=H*.47;resonance.progress=(resonance.progress+0.0048*(1+Math.min(stage,8)*.035)*(focusUntil>time?.58:1))%1;const wave=.2+Math.abs(.5-resonance.progress)*1.6;const ringR=45+wave*95;const perfect=Math.abs(resonance.progress-.5)<.055;const glow=ctx.createRadialGradient(cx,cy,5,cx,cy,105);gl