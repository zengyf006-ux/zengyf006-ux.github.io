x=input.x;input.py=input.y;input.x=p.x;input.y=p.y;}
function onUp(){input.down=false;}
function doAttack(ax,ay,bx,by,tap){
 const ang=Math.atan2(by-ay,bx-ax);let hit=false;
 if(boss&&dist(bx,by,boss.x,boss.y)<boss.r+18){hitBoss(bx,by,player.damage);hit=true;}
 for(let i=monsters.length-1;i>=0;i--){const m=monsters[i];if(m.dead)continue;const d=tap?dist(bx,by,m.x,m.y):segDist(m.x,m.y,ax,ay,bx,by);if(d<m.r+player.slashWidth){attackMonster(m,ang);hit=true;}}
 if(hit)sfx('slash');
}
canvas.addEventListener('pointerdown',onDown,{passive:false});canvas.addEventListener('pointermove',onMove,{passive:false});addEventListener('pointerup',onUp,{passive:true});addEventListener('pointercancel',onUp,{passive:true});

function drawBackground(){const g=ctx.createLinearGradient(0,0,0,H);g.addColorStop(0,'#101117');g.addColorStop(.52,'#08090d');g.addColorStop(1,'#040507');ctx.fillStyle=g;ctx.fillRect(0,0,W,H);const moon=ctx.createRadialGradient(W*.77,H*.24,10,W*.77,H*.24,Math.max(W,H)*.5);moon.addColorStop(0,'rgba(194,151,106,.09)');moon.addColorStop(.45,'rgba(101,70,73,.045)');moon.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=moon;ctx.fillRect(0,0,W,H);ctx.globalAlpha=.28;for(const s of stars){const x=s.x*W,y=(s.y*H+clock*(3+s.z*5))%H;ctx.fillStyle=s.z>.7?'#c7b993':'#fff';ctx.fillRect(x,y,1+s.z,1+s.z);}ctx.globalAlpha=1;ctx.strokeStyle='rgba(255,255,255,.022)';ctx.lineWidth=1;const step=54;ctx.beginPath();for(let y=120;y<H-110;y+=step){ctx.moveTo(0,y);ctx.lineTo(W,y)}ctx.stroke();ctx.fillStyle='rgba(245,180,95,.035)';ctx.beginPath();ctx.arc(W*.5,H-105,60,Math.PI,TAU);ctx.fill();ctx.strokeStyle='rgba(245,180,95,.11)';ctx.beginPath();ctx.arc(W*.5,H-105,52,Math.PI,TAU);ctx.stroke();}
function updateEffects(dt){for(let i=particles.length-1;i>=0;i--){const p=particles[i];p.x+=p.vx;p.y+=p.vy;p.vx*=.984;p.vy=p.vy*.984+.035;p.life-=dt*1.7;if(p.life<=0){particles.splice(i,1);continue;}ctx.globalAlpha=p.life;ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(p.x,p.y,p.size*p.life,0,TAU);ctx.fill();}ctx.globalAlpha=1;for(let i=rings.length-1;i>=0;i--){const r=rings[i];r.life-=dt*1.6;r.r+=dt*82;if(r.life<=0){rings.splice(i,1);continue;}ctx.globalAlpha=r.life;ctx.strokeStyle=r.color;ctx.lineWidth=r.w;ctx.beginPath();ctx.arc(r.x,r.y,r.r,0,TAU);ctx.stroke();}ctx.globalAlpha=1;for(let i=trails.length-1;i>=0;i--){const t=trails[i];t.life-=dt*(t.lightning?4.2:3.8);if(t.life<=0){trails.splice(i,1);continue;}ctx.globalAlpha=t.life;ctx.strokeStyle=t.lightning?colors.cyan:colors.amber;ctx.lineWidth=t.lightning?2:2+6*t.life;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(t.ax,t.ay);if(t.lightning){const mx=(t.ax+t.bx)/2+rand(-8,8),my=(t.ay+t.by)/2+rand(-8,8);ctx.lineTo(mx,my);ctx.lineTo(t.bx,t.by);}else ctx.lineTo(t.bx,t.by);ctx.stroke();}ctx.globalAlpha=1;}
function updateGame(dt){
 if(state.paused)return;state.waveTime+=dt;if(speechCooldown>0)speechCooldown-=dt;if(state.comboTimer>0){state.comboTimer-=dt;if(state.comboTimer<=0){state.chain=0;state.combo=1;}}
 if(state.skillCooldown>0)state.skillCooldown-=dt;
 const idx=(state.wave-1)%4;if(idx!==3){const desired=Math.min(20,Math.round((6+Math.floor(state.wave*.7))*currentModifier.spawn));while(monsters.filter(m=>!m.dead).length<desired&&Math.random()<.18)spawnMonster(null,state.wave>2&&Math.random()<.08);if(state.waveTime>=state.waveDuration)endWave();}
 for(const m of monsters)if(!m.dead)m.update(dt);for(let i=monsters.length-1;i>=0;i--)if(monsters[i].dead)monsters.splice(i,1);if(boss)updateBoss(dt);
}
function drawGame(){monsters.forEach(m=>m.draw());if(boss)drawBoss();}
function updateHUD(){ui.score.textContent=state.score;ui.combo.textContent='×'+state.combo;ui.level.textContent=state.level;ui.xpLabel.textContent=state.xp+' / '+state.xpNeed+' XP';ui.xpFill.style.width=(state.xp/state.xpNeed*100)+'%';ui.damageValue.textContent=player.damage;ui.critValue.textContent=Math.round(player.crit*100)+'%';ui.chainValue.textContent=player.chain;ui.skillBtn.classList.toggle('locked',state.skill<100);ui.skillText.textContent=state.skill>=100?'可以使用':Math.round(state.skill)+'%';ui.voiceState.textContent=state.voice?(humanPack?'真人语音':'系统语音'):'语音关闭';}
function frame(ts){if(!state.running)return;if(!last)last=ts;const dt=Math.min(.033,(ts-last)/1000);last=ts;clock+=dt;updateGame(dt);const sx=state.shake>0?rand(-state.shake,state.shake):0,sy=state.shake>0?rand(-state.shake,state.shake):0;state.shake=Math.max(0,state.shake-dt*24);ctx.save();ctx.translate(sx,sy);drawBackground();drawGame();updateEffects(dt);ctx.restore();if(state.flash>0){state.flash=Math.max(0,state.flash-dt*2.8);ctx.fillStyle='rgba(255,245,230,'+(state.flash*.42)+')';ctx.fillRect(0,0,W,H);}updateHUD();requestAnimationFrame(frame);}

function start(quiet=false){state.sound=!quiet;state.voice=!quiet;initAudio();ui.intro.classList.add('hidden');state.running=true;resetAll();last=0;if(!quiet){sfx('level');setTimeout(()=>voice('start',true),280);}requestAnimationFrame(frame);}
ui.startBtn.addEventListener('click',()=>start(false));ui.quietBtn.addEventListener('click',()=>start(true));ui.skillBtn.addEventListener('click',useSkill);ui.voiceBtn.addEventListener('click',()=>{state.voice=!state.voice;if(!state.voice&&'speechSynthesis'in window)speechSynthesis.cancel();message(state.voice?'语音已开启':'语音已关闭');updateHUD();});ui.pauseBtn.addEventListener('click',()=>{if(!state.running)return;state.paused=true;ui.pause.classList.remove('hidden');if('speechSynthesis'in window)speechSynthesis.cancel();});ui.resumeBtn.addEventListener('click',()=>{ui.pause.classList.add('hidden');state.paused=false;last=performance.now();});ui.restartBtn.addEventListener('click',()=>{ui.pause.classList.add('hidden');resetAll();last=performance.now();});
document.addEventListener('visibilitychange',()=>{if(document.hidden){last=0;if(initAudio.ctx?.state==='running')initAudio.ctx.suspend();}else if(state.running){last=0;if(state.sound)initAudio.ctx?.resume();}});
loadHumanPack();updateHUD();
})();
