(m){ctx.rotate(Math.sin(m.age*3)*.04);ctx.fillStyle='#6d4a27';ctx.strokeStyle='#e1b36e';ctx.lineWidth=2;ctx.beginPath();roundRectPath(-m.r,-m.r*.45,m.r*2,m.r*1.2,6);ctx.fill();ctx.stroke();ctx.fillStyle='#d1a25e';ctx.fillRect(-m.r,m.r*.05,m.r*2,4);ctx.fillStyle='#302015';ctx.fillRect(-4,-m.r*.1,8,12);if(m.mimicState!=='sleep'){ctx.fillStyle='#130b0b';ctx.beginPath();ctx.ellipse(0,m.r*.28,m.r*.7,m.r*.42,0,0,TAU);ctx.fill();ctx.fillStyle='#efe2cc';for(let i=-2;i<=2;i++){ctx.beginPath();ctx.moveTo(i*8-3,m.r*.02);ctx.lineTo(i*8+2,m.r*.23);ctx.lineTo(i*8+5,m.r*.02);ctx.fill();}ctx.fillStyle='#f5b45f';ctx.beginPath();ctx.arc(-8,-m.r*.18,3,0,TAU);ctx.arc(8,-m.r*.18,3,0,TAU);ctx.fill();}}
function drawLeech(m){ctx.rotate(Math.atan2(m.gateY-m.y,m.gateX-m.x)+Math.PI/2);ctx.strokeStyle='#e45f67';ctx.lineWidth=7;ctx.lineCap='round';ctx.beginPath();for(let i=0;i<6;i++){const y=-m.r+i*(m.r*2/5),x=Math.sin(m.age*8+i*.8)*4;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.stroke();ctx.fillStyle='#2a1014';ctx.beginPath();ctx.arc(0,-m.r,8,0,TAU);ctx.fill();ctx.strokeStyle='#f0a0a7';ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,-m.r,5,0,TAU);ctx.stroke();}
function roundRectPath(x,y,w,h,r){ctx.beginPath();ctx.moveTo(x+r,y);ctx.arcTo(x+w,y,x+w,y+h,r);ctx.arcTo(x+w,y+h,x,y+h,r);ctx.arcTo(x,y+h,x,y,r);ctx.arcTo(x,y,x+w,y,r);ctx.closePath();}

function spawnMonster(forceType=null,elite=false){if(monsters.length>=MAX_MONSTERS)return;let pool;if(state.wave%4===1)pool=['slime','slime','bat','leech'];else if(state.wave%4===2)pool=['slime','bat','burrow','burrow','leech'];else pool=['bat','burrow','beetle','healer','mimic','leech'];if(currentModifier.id==='swarm')pool=pool.concat(['slime','slime','bat','bat']);if(currentModifier.id==='armor')pool=pool.concat(['beetle','beetle']);if(currentModifier.id==='plague')pool=pool.concat(['healer','healer']);const type=forceType||pool[(Math.random()*pool.length)|0];monsters.push(new Monster(type,elite));announceType(type);}
function spawnBoss(){const max=140+state.wave*28;boss={x:W/2,y:H*.39,r:Math.min(120,Math.max(88,W*.23)),hp:max,maxHp:max,phase:1,age:0,weak:0,weakTimer:0,flash:0,summonTimer:3.5,shield:0,rot:0};ui.bossHud.classList.remove('hidden');ui.bossName.textContent='空洞王';ui.bossPhase.textContent='第一形态 · 凝视';voice('boss',true);sfx('boss');}
function updateBoss(dt){if(!boss)return;boss.age+=dt;boss.rot+=dt*(.15+boss.phase*.05);boss.weakTimer-=dt;boss.summonTimer-=dt;if(boss.weakTimer<=0){boss.weak=(boss.weak+1+((Math.random()*4)|0))%6;boss.weakTimer=Math.max(1.2,2.5-boss.phase*.25);}if(boss.summonTimer<=0){boss.summonTimer=Math.max(2.2,4.6-boss.phase*.45);for(let i=0;i<1+boss.phase;i++)spawnMonster(boss.phase>=3?'mimic':(Math.random()<.5?'bat':'slime'),boss.phase>=3&&Math.random()<.25);}const ratio=boss.hp/boss.maxHp;const next=ratio<.33?3:ratio<.66?2:1;if(next!==boss.phase){boss.phase=next;boss.flash=1;sfx('boss');message('空洞王进入第 '+next+' 形态',1200);ui.bossPhase.textContent=next===2?'第二形态 · 召唤':next===3?'第三形态 · 狂暴':'第一形态 · 凝视';if(next===2)boss.shield=3;if(next===3)boss.summonTimer=.6;}
 ui.bossFill.style.width=(ratio*100)+'%';
}
function hitBoss(x,y,damage){if(!boss)return;const a=(Math.atan2(y-boss.y,x-boss.x)-boss.rot+TAU)%TAU,seg=Math.floor((a+TAU/12)/(TAU/6))%6;if(seg!==boss.weak){sfx('miss');addRing(x,y,colors.red,8,2);message('弱点在别处');return;}if(boss.shield>0){boss.shield--;message('王座护盾破损');sfx('hit',1);return;}const crit=Math.random()<player.crit;const dmg=Math.max(1,Math.round(damage*(crit?2:1)));boss.hp-=dmg;boss.flash=1;state.score+=dmg*3*state.combo;state.chain++;state.combo=1+Math.floor(state.chain/6);state.comboTimer=player.comboGrace;state.skill=clamp(state.skill+3*player.skillGain,0,100);addParticles(x,y,crit?colors.amber:colors.red,crit?26:16,crit?1.6:1.1);addRing(x,y,crit?colors.amber:colors.red,18,3);sfx(crit?'crit':'boss');vibrate([10,14,10]);if(boss.hp<=0){state.score+=250+state.wave*20;state.skill=100;addParticles(boss.x,boss.y,colors.amber,80,2.2);addRing(boss.x,boss.y,colors.amber,boss.r*.7,5);state.flash=.8;state.shake=14;sfx('skill');voice('bossKill',true);boss=null;ui.bossHud.classList.add('hidden');state.waveCleared=true;setTimeout(endWave,1200);}}
function drawBoss(){if(!boss)return;const x=boss.x,y=boss.y,r=boss.r,ratio=boss.hp/boss.maxHp;const glow=ctx.createRadialGradient(x,y,10,x,y,r*1.8);glow.addColorStop(0,'rgba(228,95,103,.23)');glow.addColorStop(.5,'rgba(145,120,232,.08)');glow.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=glow;ctx.beginPath();ctx.arc(x,y,r*1.8,0,TAU);ctx.fill();ctx.save();ctx.translate(x,y);ctx.rotate(boss.rot);for(let i=0;i<6;i++){ctx.save();ctx.rotate(i*TAU/6);const active=i===boss.weak;ctx.fillStyle=active?'rgba(245,180,95,.15)':'rgba(255,255,255,.025)';ctx.strokeStyle=active?colors.amber:'rgba(255,255,255,.16)';ctx.lineWidth=active?4:2;ctx.beginPath();ctx.arc(0,0,r,-TAU/12,TAU/12);ctx.lineTo(Math.cos(TAU/12)*r*.66,Math.sin(TAU/12)*r*.66);ctx.arc(0,0,r*.66,TAU/12,-TAU/12,true);ctx.closePath();ctx.fill();ctx.stroke();if(active){ctx.fillStyle=colors.amber;ctx.beginPath();ctx.arc(r*.84,0,5+Math.sin(clock*6)*2,0,TAU);ctx.fill();}ctx.restore();}ctx.restore();ctx.fillStyle='#1a1117';ctx.beginPath();ctx.ellipse(x,y+8,r*.58,r*.7,0,0,TAU);ctx.fill();ctx.strokeStyle='rgba(255,255,255,.16)';ctx.lineWidth=2;ctx.stroke();ctx.fillStyle=colors.red;ctx.beginPath();ctx.arc(x-r*.2,y-r*.1,6,0,TAU);ctx.arc(x+r*.2,y-r*.1,6,0,TAU);ctx.fill();ctx.strokeStyle='#e7d7c2';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(x-r*.48,y-r*.38);ctx.lineTo(x-r*.72,y-r*.82);ctx.moveTo(x+r*.48,y-r*.38);ctx.lineTo(x+r*.72,y-r*.82);ctx.stroke();ctx.fillStyle='#0b0709';ctx.beginPath();ctx.ellipse(x,y+r*.22,r*.32,r*.18,0,0,TAU);ctx.fill();if(boss.shield>0){ctx.strokeStyle=colors.cyan;ctx.lineWidth=4;ctx.globalAlpha=.65+.2*Math.sin(clock*5);ctx.beginPath();ctx.arc(x,y,r*.82,0,TAU);ctx.stroke();ctx.globalAlpha=1;}
}

function gainXp(n){state.xp+=Math.round(n*player.xpGain*currentModifier.xp);while(state.xp>=s