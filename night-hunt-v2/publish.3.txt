s.mimicTimer<=0){this.mimicState='awake';this.mimicTimer=.65;}else if(this.mimicState==='awake'&&this.mimicTimer<=0){this.mimicState='dash';this.dash=1.1;}if(this.mimicState==='dash'){speed*=3.2;this.dash-=dt;if(this.dash<=0){this.mimicState='sleep';this.teleportTimer=rand(1.4,2.5);this.echoLife=0;this.echoX=0;this.echoY=0;this.mimicTimer=rand(.7,1.3)}}else speed*=.18;this.x+=dx/d*speed*dt;this.y+=dy/d*speed*dt;}
  else if(this.type==='shade'){this.teleportTimer-=dt;this.echoLife=Math.max(0,this.echoLife-dt);if(this.teleportTimer<=0){this.echoX=this.x;this.echoY=this.y;this.echoLife=.7;this.x=clamp(this.x+dx/d*rand(45,85)+rand(-32,32),28,W-28);this.y=clamp(this.y+dy/d*rand(35,70)+rand(-20,20),120,H-175);this.teleportTimer=rand(1.25,2.15);addRing(this.x,this.y,this.color,10,2);}this.x+=dx/d*speed*.55*dt+Math.sin(this.age*5)*10*dt;this.y+=dy/d*speed*.55*dt;}
  else {this.phase+=dt*6;this.x+=dx/d*speed*dt+Math.sin(this.phase)*7*dt;this.y+=dy/d*speed*dt;}
  if(dist(this.x,this.y,this.gateX,this.gateY)<24){this.escape();}
 }
 escape(){if(this.dead)return;this.dead=true;state.misses++;state.chain=0;state.combo=1;state.comboTimer=0;const penalty=applyEscapePenalty(this.type,state.skill,state.gate);state.skill=penalty.skill;state.gate=penalty.gate;state.shake=Math.max(state.shake,7);state.flash=Math.max(state.flash,.18);message(this.type==='leech'?'噬能虫偷走能量，夜门受损':'夜门受到冲击');sfx('miss');vibrate([10,18,10]);if(state.gate<=2&&!state.gameOver)voice('gate');if(state.gate<=0)finishRun();else if(state.misses===4)voice('danger');}
 hit(dmg,attackAngle=0,crit=false){
  if(this.dead||this.hitCd>0)return false;if(this.type==='burrow'&&this.submerge){this.hitCd=.24;message('它还在地下');return false;}
  this.hitCd=player.attackCd*(state.feverUntil>clock?.55:1);let blocked=false;this.lastBlocked=false;
  if(this.shield>0){this.shield--;blocked=true;addText(this.x,this.y-this.r,'护甲',colors.cyan,10);addRing(this.x,this.y,colors.cyan,this.r*.6,3);message('甲壳破损');sfx('hit',.7);}
  else {this.hp-=dmg;addText(this.x,this.y-this.r,String(dmg),crit?colors.amber:colors.ink,crit?16:12);if(player.slow>0)this.slowTimer=Math.max(this.slowTimer||0,1.1+player.slow*.35);if(player.execute>0&&this.hp>0&&this.hp/this.maxHp<=player.execute)this.hp=0;this.flash=1;sfx('hit',dmg);addParticles(this.x,this.y,this.color,8+Math.min(10,state.combo),1);addRing(this.x,this.y,this.color,this.r*.4,2);}
  this.lastBlocked=blocked;if(!blocked&&this.hp<=0)this.kill();return true;
 }
 kill(){
  if(this.dead)return;this.dead=true;state.kills++;state.chain++;state.combo=1+Math.floor(state.chain/6);state.comboTimer=player.comboGrace;state.score+=Math.round(this.score*state.combo*(state.feverUntil>clock?2:1));state.skill=clamp(state.skill+7*player.skillGain,0,100);gainXp(monsterInfo[this.type].xp*(this.elite?1.7:1));addParticles(this.x,this.y,this.color,this.elite?28:18,this.elite?1.7:1.2);addRing(this.x,this.y,this.color,this.r,3);vibrate(this.elite?[12,16,12]:8);
  if(this.type==='slime'&&this.splitDepth<1){const count=childSpawnCount(monsters.filter(m=>!m.dead).length,MAX_MONSTERS,2);for(let i=0;i<count;i++){const m=new Monster('slime',false,this);m.r=this.r*.62;m.maxHp=m.hp=1;m.speed*=1.3;monsters.push(m)}}if(this.affix==='brood'){const count=childSpawnCount(monsters.filter(m=>!m.dead).length,MAX_MONSTERS,2);for(let i=0;i<count;i++){const m=new Monster(Math.random()<.5?'bat':'slime',false,this);m.r*=.75;m.maxHp=m.hp=1;m.speed*=1.25;monsters.push(m)}}
  if(this.type==='healer')message('治愈灵已清除');if(this.elite){message('异化怪击杀');voice('elite');}
  if(state.chain===10)voice('combo10');if(state.chain===25)voice('combo25');if(state.chain>=30&&state.feverUntil<=clock){state.fever=100;state.feverUntil=clock+8;document.getElementById('app').classList.add('fever');voice('fever',true);message('猎杀狂热 · 8 秒双倍得分',1200);}
  applyOnKill(this);
 }
 draw(){
  const eliteScale=this.elite?1.08:1;ctx.save();ctx.translate(this.x,this.y);ctx.scale(eliteScale,eliteScale);if(this.flash>0){ctx.shadowColor='#fff';ctx.shadowBlur=18;}
  if(this.elite){const info=affixInfo[this.affix]||affixInfo.frenzy;ctx.strokeStyle=info.color;ctx.lineWidth=2;ctx.globalAlpha=.7+.2*Math.sin(clock*5);ctx.beginPath();ctx.arc(0,0,this.r*1.32,0,TAU);ctx.stroke();ctx.globalAlpha=1;ctx.fillStyle=info.color;ctx.font='800 9px -apple-system,BlinkMacSystemFont,"PingFang SC"';ctx.textAlign='center';ctx.fillText(info.mark+' '+info.name,0,-this.r-13);}
  if(this.type==='slime')drawSlime(this);else if(this.type==='bat')drawBat(this);else if(this.type==='burrow')drawBurrow(this);else if(this.type==='beetle')drawBeetle(this);else if(this.type==='healer')drawHealer(this);else if(this.type==='mimic')drawMimic(this);else if(this.type==='shade')drawShade(this);else drawLeech(this);
  if(this.maxHp>2&&!this.dead){ctx.fillStyle='rgba(0,0,0,.5)';ctx.fillRect(-this.r,this.r+9,this.r*2,3);ctx.fillStyle=this.hp/this.maxHp>.45?colors.red:colors.amber;ctx.fillRect(-this.r,this.r+9,this.r*2*this.hp/this.maxHp,3);}
  ctx.restore();
 }
}

function drawSlime(m){const squash=.9+Math.sin(m.age*5+m.phase)*.13;ctx.scale(1/squash,squash);const g=ctx.createLinearGradient(0,-m.r,0,m.r);g.addColorStop(0,'#8be2ad');g.addColorStop(1,'#315a49');ctx.fillStyle=g;ctx.beginPath();ctx.moveTo(-m.r*.9,m.r*.5);ctx.bezierCurveTo(-m.r*1.05,-m.r*.45,-m.r*.55,-m.r*.9,0,-m.r*.86);ctx.bezierCurveTo(m.r*.58,-m.r*.92,m.r*1.02,-m.r*.34,m.r*.9,m.r*.5);ctx.quadraticCurveTo(m.r*.35,m.r*.9,0,m.r*.66);ctx.quadraticCurveTo(-m.r*.38,m.r*.9,-m.r*.9,m.r*.5);ctx.fill();ctx.fillStyle='#0b1712';ctx.beginPath();ctx.arc(-m.r*.28,-m.r*.18,3.3,0,TAU);ctx.arc(m.r*.28,-m.r*.18,3.3,0,TAU);ctx.fill();ctx.strokeStyle='#19372c';ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,m.r*.14,m.r*.22,.15*Math.PI,.85*Math.PI);ctx.stroke();}
function drawBat(m){const flap=Math.sin(m.age*12+m.phase);ctx.fillStyle='#20182b';ctx.strokeStyle='#9c7ee3';ctx.lineWidth=1.5;ctx.beginPath();ctx.ellipse(0,0,13,18,0,0,TAU);ctx.fill();ctx.stroke();ctx.save();ctx.scale(1,.72+.28*Math.abs(flap));ctx.fillStyle='#3a274c';ctx.beginPath();ctx.moveTo(-8,-3);ctx.quadraticCurveTo(-34,-25,-43,0);ctx.quadraticCurveTo(-27,-8,-11,12);ctx.closePath();ctx.fill();ctx.beginPath();ctx.moveTo(8,-3);ctx.quadraticCurveTo(34,-25,43,0);ctx.quadraticCurveTo(27,-8,11,12);ctx.closePath();ctx.fill();ctx.restore();ctx.fillStyle='#f2b46b';ctx.beginPath();ctx.arc(-4,-3,2.5,0,TAU);ctx.arc(4,-3,2.5,0,TAU);ctx.fill();ctx.strokeStyle='#c7b6df';ctx.beginPath();ctx.moveTo(-5,10);ctx.lineTo(-2,15);ctx.moveTo(5,10);ctx.lineTo(2,15);ctx.stroke();}
function drawBurrow(m){if(m.submerge){ctx.strokeStyle='rgba(179,138,99,.75)';ctx.lineWidth=2;for(let i=0;i<3;i++){ctx.beginPath();ctx.ellipse(0,0,m.r*(.5+i*.35),m.r*(.16+i*.08),0,0,TAU);ctx.stroke();}return;}ctx.fillStyle='#5e4536';ctx.beginPath();ctx.ellipse(0,3,m.r*.82,m.r,0,0,TAU);ctx.fill();ctx.fillStyle='#b89370';ctx.beginPath();ctx.ellipse(0,-5,m.r*.55,m.r*.62,0,0,TAU);ctx.fill();ctx.fillStyle='#17110d';ctx.beginPath();ctx.arc(-6,-8,2.5,0,TAU);ctx.arc(6,-8,2.5,0,TAU);ctx.fill();ctx.fillStyle='#df9d91';ctx.beginPath();ctx.arc(0,0,4,0,TAU);ctx.fill();ctx.strokeStyle='#d8c0a4';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(-m.r*.65,5);ctx.lineTo(-m.r*1.1,14);ctx.moveTo(m.r*.65,5);ctx.lineTo(m.r*1.1,14);ctx.stroke();}
function drawBeetle(m){ctx.rotate(m.rot+Math.PI/2);ctx.fillStyle='#263945';ctx.beginPath();ctx.ellipse(0,0,m.r*.72,m.r,0,0,TAU);ctx.fill();ctx.strokeStyle='#6eddec';ctx.lineWidth=2;ctx.stroke();ctx.beginPath();ctx.moveTo(0,-m.r*.82);ctx.lineTo(0,m.r*.82);ctx.stroke();for(let y=-10;y<=10;y+=10){ctx.beginPath();ctx.moveTo(-m.r*.55,y);ctx.lineTo(-m.r*1.05,y+5);ctx.moveTo(m.r*.55,y);ctx.lineTo(m.r*1.05,y+5);ctx.stroke();}if(m.shield>0){ctx.fillStyle='rgba(110,221,236,.2)';ctx.strokeStyle='#9af4ff';ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,-m.r*.8,m.r*.82,Math.PI,TAU);ctx.fill();ctx.stroke();ctx.fillStyle='#d9fbff';ctx.font='800 9px sans-serif';ctx.textAlign='center';ctx.fillText(m.shield,0,-m.r*.9);}}
function drawHealer(m){const bob=Math.sin(m.age*3+m.phase)*4;ctx.translate(0,bob);const g=ctx.createRadialGradient(0,-4,2,0,0,m.r*1.2);g.addColorStop(0,'rgba(190,220,255,.95)');g.addColorStop(.45,'rgba(118,168,255,.55)');g.addColorStop(1,'rgba(118,168,255