i*TAU/6);const active=i===boss.weak;ctx.fillStyle=active?'rgba(245,180,95,.15)':'rgba(255,255,255,.025)';ctx.strokeStyle=active?colors.amber:'rgba(255,255,255,.16)';ctx.lineWidth=active?4:2;ctx.beginPath();ctx.arc(0,0,r,-TAU/12,TAU/12);ctx.lineTo(Math.cos(TAU/12)*r*.66,Math.sin(TAU/12)*r*.66);ctx.arc(0,0,r*.66,TAU/12,-TAU/12,true);ctx.closePath();ctx.fill();ctx.stroke();if(active){ctx.fillStyle=colors.amber;ctx.beginPath();ctx.arc(r*.84,0,5+Math.sin(clock*6)*2,0,TAU);ctx.fill();}ctx.restore();}ctx.restore();ctx.fillStyle='#1a1117';ctx.beginPath();ctx.ellipse(x,y+8,r*.58,r*.7,0,0,TAU);ctx.fill();ctx.strokeStyle='rgba(255,255,255,.16)';ctx.lineWidth=2;ctx.stroke();ctx.fillStyle=colors.red;ctx.beginPath();ctx.arc(x-r*.2,y-r*.1,6,0,TAU);ctx.arc(x+r*.2,y-r*.1,6,0,TAU);ctx.fill();ctx.strokeStyle='#e7d7c2';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(x-r*.48,y-r*.38);ctx.lineTo(x-r*.72,y-r*.82);ctx.moveTo(x+r*.48,y-r*.38);ctx.lineTo(x+r*.72,y-r*.82);ctx.stroke();ctx.fillStyle='#0b0709';ctx.beginPath();ctx.ellipse(x,y+r*.22,r*.32,r*.18,0,0,TAU);ctx.fill();if(boss.shield>0){ctx.strokeStyle=colors.cyan;ctx.lineWidth=4;ctx.globalAlpha=.65+.2*Math.sin(clock*5);ctx.beginPath();ctx.arc(x,y,r*.82,0,TAU);ctx.stroke();ctx.globalAlpha=1;}
}

function drawWeaverBoss(){const x=boss.baseX+Math.sin(boss.age*.8)*18,y=boss.baseY+Math.cos(boss.age*.65)*10,r=boss.r;boss.x=x;boss.y=y;const glow=ctx.createRadialGradient(x,y,10,x,y,r*1.9);glow.addColorStop(0,'rgba(200,141,255,.2)');glow.addColorStop(.5,'rgba(110,80,170,.07)');glow.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=glow;ctx.beginPath();ctx.arc(x,y,r*1.9,0,TAU);ctx.fill();ctx.save();ctx.translate(x,y);ctx.rotate(boss.rot);ctx.strokeStyle='rgba(206,177,230,.45)';ctx.lineWidth=5;ctx.lineCap='round';for(let i=0;i<8;i++){const a=i*TAU/8;ctx.beginPath();ctx.moveTo(Math.cos(a)*r*.45,Math.sin(a)*r*.45);ctx.quadraticCurveTo(Math.cos(a+.18)*r*.92,Math.sin(a+.18)*r*.92,Math.cos(a)*r*1.28,Math.sin(a)*r*1.28);ctx.stroke();}ctx.fillStyle='#211326';ctx.strokeStyle='#b88be5';ctx.lineWidth=2;ctx.beginPath();ctx.ellipse(0,6,r*.55,r*.7,0,0,TAU);ctx.fill();ctx.stroke();ctx.beginPath();ctx.arc(0,-r*.42,r*.32,0,TAU);ctx.fill();ctx.stroke();for(let i=0;i<boss.segments;i++){const a=i*TAU/boss.segments,active=i===boss.weak;ctx.fillStyle=active?colors.amber:'#c88dff';ctx.shadowColor=active?colors.amber:'#9c6ccc';ctx.shadowBlur=active?18:8;ctx.beginPath();ctx.arc(Math.cos(a)*r*.27,-r*.42+Math.sin(a)*r*.18,active?7:4,0,TAU);ctx.fill();}ctx.shadowBlur=0;ctx.restore();const wp=bossWeakPoint();ctx.fillStyle=colors.amber;ctx.shadowColor=colors.amber;ctx.shadowBlur=22;ctx.beginPath();ctx.arc(wp.x,wp.y,8+Math.sin(clock*7)*2,0,TAU);ctx.fill();ctx.shadowBlur=0;ctx.strokeStyle='rgba(245,180,95,.45)';ctx.lineWidth=2;ctx.beginPath();ctx.arc(wp.x,wp.y,18,0,TAU);ctx.stroke();const hp=boss.hp/boss.maxHp;ctx.strokeStyle='rgba(255,255,255,.09)';ctx.lineWidth=7;ctx.beginPath();ctx.arc(x,y,r*.72,-Math.PI/2,-Math.PI/2+TAU);ctx.stroke();ctx.strokeStyle=hp>.35?'#c88dff':colors.amber;ctx.beginPath();ctx.arc(x,y,r*.72,-Math.PI/2,-Math.PI/2+TAU*hp);ctx.stroke();if(boss.shield>0){ctx.strokeStyle=colors.cyan;ctx.lineWidth=4;ctx.beginPath();ctx.arc(x,y,r*.87,0,TAU);ctx.stroke();}if(boss.attackState==='charge'){const maxCharge=Math.max(.85,1.45-boss.phase*.15),p=clamp(1-boss.attackCharge/maxCharge,0,1);ctx.strokeStyle=colors.red;ctx.lineWidth=3+4*p;ctx.beginPath();ctx.arc(x,y,r*(1.02+.32*p),0,TAU);ctx.stroke();ctx.fillStyle=colors.red;ctx.font='900 11px -apple-system,BlinkMacSystemFont,"PingFang SC"';ctx.textAlign='center';ctx.fillText('撕裂蛛网前打断',x,y-r-28);}}
function gainXp(n){state.xp+=Math.round(n*player.xpGain*currentModifier.xp);if(state.xp>=state.xpNeed&&!state.choosing){state.xp-=state.xpNeed;state.level++;state.xpNeed=Math.round(state.xpNeed*1.28+8);openUpgrade();}}
function applyOnKill(m){if(player.pulse>0&&Math.random()<player.pulseChance*player.pulse){const near=monsters.filter(o=>!o.dead&&dist(o.x,o.y,m.x,m.y)<90).slice(0,2+player.pulse);near.forEach(o=>o.hit(Math.max(1,Math.round(player.damage*.7))));addRing(m.x,m.y,colors.violet,18,3);} }
function attackMonster(m,angle=0){const crit=Math.random()<player.crit;const dmg=Math.max(1,Math.round(player.damage*(crit?2:1)));const hit=m.hit(dmg,angle,crit);if(!hit)return;if(crit&&!m.lastBlocked){state.hitStop=.035;sfx('crit');addParticles(m.x,m.y,colors.amber,18,1.5);message('暴击');}if(!m.lastBlocked&&player.chain>0&&Math.random()<player.chainChance){const candidates=monsters.filter(o=>o!==m&&!o.dead&&dist(o.x,o.y,m.x,m.y)<150).sort((a,b)=>dist(a.x,a.y,m.x,m.y)-dist(b.x,b.y,m.x,m.y)).slice(0,player.chain);const token=runToken;candidates.forEach((o,i)=>setTimeout(()=>{if(token===runToken&&!o.dead){o.hit(Math.max(1,Math.round(player.damage*.65)));addLightning(m.x,m.y,o.x,o.y)}},i*45));sfx('chain');}}

const upgrades=[
 {id:'damage',tag:'裂影系',glyph:'╱',title:'锋刃加压',desc:'基础伤害 +1。对甲壳和 Boss 的击穿能力明显提高。',max:6,color:'#f5b45f',apply:()=>player.damage++},
 {id:'width',tag:'裂影系',glyph:'⟋',title:'宽幅斩击',desc:'拖动攻击的判定范围扩大，连切怪群更顺手。',max:5,color:'#e28f72',apply:()=>player.slashWidth+=5},
 {id:'crit',tag:'猎杀系',glyph:'✦',title:'致命直觉',desc:'暴击率 +8%，暴击会产生更重的声音和震动。',max:6,color:'#ff6f86',apply:()=>player.crit=Math.min(.55,player.crit+.08)},
 {id:'chain',tag:'雷弧系',glyph:'ϟ',title:'弧光跳跃',desc:'命中时有概率连锁附近怪物，每级增加一个连锁目标。',max:4,color:'#6eddec',apply:()=>player.chain++},
 {id:'pulse',tag:'震荡系',glyph:'◎',title:'余震核心',desc:'击杀时有概率产生范围冲击，自动伤害周围怪物。',max:4,color:'#9178e8',apply:()=>player.pulse++},
 {id:'skill',tag:'能量系',glyph:'◈',title:'快速蓄能',desc:'攻击与击杀获得更多技能能量，更频繁释放震荡脉冲。',max:5,color:'#65d69b',apply:()=>player.skillGain+=.22},
 {id:'xp',tag:'成长系',glyph:'△',title:'猎场领悟',desc:'经验获取 +20%，更快进入下一次三选一进化。',max:4,color:'#d7c58f',apply:()=>player.xpGain+=.2},
 {id:'combo',tag:'节奏系',glyph:'∞',title:'延长猎杀窗口',desc:'连击中断时间延长 0.35 秒，更容易维持高倍率。',max:4,color:'#9fa8ff',apply:()=>player.comboGrace+=.35},
 {id:'frost',tag:'控制系',glyph:'❄',title:'寒蚀刃痕',desc:'命中会短暂减速怪物，为地穴兽和冲刺怪创造攻击窗口。',max:3,color:'#8bdcff',apply:()=>player.slow++},
 {id:'execute',tag:'处决系',glyph:'◇',title:'虚弱处决',desc:'低生命怪物会被直接处决，每级提高处决阈值。',max:3,color:'#e8849a',apply:()=>player.execute=Math.min(.32,player.execute+.08)},
 {id:'gate',tag:'守门系',glyph:'▣',title:'夜门加固',desc:'夜门上限 +1，并立即修复 2 点耐久。',max:3,color:'#b9c8d8',apply:()=>{state.maxGate++;state.gate=Math.min(state.maxGate,state.gate+2)}},
 {id:'boss',tag:'王座系',glyph:'⌾',title:'弱点猎手',desc:'对 Boss 弱点造成的伤害提高 15%。',max:4,color:'#f0a36d',apply:()=>player.bossDamage+=.15}
];
const fallbackUpgrades=[
 {id:'repair',tag:'补给',glyph:'＋',title:'紧急修复',desc:'恢复 2 点夜门耐久。',max:999,color:'#b9c8d8',apply:()=>state.gate=Math.min(state.maxGate,state.gate+2)},
 {id:'charge',tag:'补给',glyph:'◈',title:'满额蓄能',desc:'立即充满震荡脉冲。',max:999,color:'#65d69b',apply:()=>state.skill=100},
 {id:'cache',tag:'补给',glyph:'✦',title:'猎杀秘藏',desc:'立刻获得 300 × 当前波次的猎杀分。',max:999,color:'#f5b45f',apply:()=>state.score+=300*state.wave}
];
const upLevel={};
let currentUpgradePicks=[];
function rarityForUpgrade(){const roll=Math.random()+Math.min(.18,state.wave*.012);if(roll>.97)return {id:'legendary',name:'传说',steps:2};if(roll>.82)return {id:'epic',name:'史诗',steps:2};if(roll>.58)return {id:'rare',name:'稀有',steps:1};return {id:'common',name:'普通',steps:1};}
function maybeEvolve(){if(player.evolution)return;const evo=resolveEvolution(upLevel);if(!evo)return;player.evolution=evo;player.weapon=evo;if(evo==='月蚀巨刃'){player.damage+=2;player.slashWidth+=12;player.bossDamage+=.25;}else if(evo==='雷狱猎刃'){player.chainChance=.42;player.crit=Math.min(.65,player.crit+.1);player.chain+=1;}else if(evo==='虚空震刃'){player.pulseChance=.24;player.pulse+=1;player.skillGain+=.35;}message(evo+' 已觉醒',1500);voice('evolve',true);state.flash=.55;sfx('level');}
function renderUpgradeChoices(){
 currentUpgradePicks=pickUpgradeOptions(upgrades,upLevel,fallbackUpgrades,3,Math.random);const routeIds=selectedLoadout==='arc'?['chain','crit','combo']:selectedLoadout==='pulse'?['pulse','skill','xp']:['damage','width','boss'];if(!currentUpgradePicks.some(u=>routeIds.includes(u.id))){const route=upgrades.filter(u=>routeIds.includes(u.id)&&(upLevel[u.id]||0)<u.max&&!currentUpgradePicks.some(p=>p.id===u.id));if(route.length)currentUpgradePi