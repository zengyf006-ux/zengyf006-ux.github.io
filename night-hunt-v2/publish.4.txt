,0)');ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,m.r*1.25,0,TAU);ctx.fill();ctx.strokeStyle='#9fbeff';ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(0,0,m.r*.68,0,TAU);ctx.stroke();ctx.fillStyle='#f4f7ff';ctx.beginPath();ctx.arc(-5,-2,2.5,0,TAU);ctx.arc(5,-2,2.5,0,TAU);ctx.fill();ctx.strokeStyle='#86a8e8';for(let i=0;i<4;i++){ctx.beginPath();ctx.moveTo(-10+i*7,m.r*.55);ctx.quadraticCurveTo(-12+i*8,m.r*.95+Math.sin(m.age*5+i)*4,-15+i*10,m.r*1.22);ctx.stroke();}ctx.globalAlpha=.45+.25*Math.sin(m.age*4);ctx.strokeStyle=colors.green;ctx.beginPath();ctx.arc(0,0,m.r*1.45,0,TAU);ctx.stroke();ctx.globalAlpha=1;}
function drawMimic(m){ctx.rotate(Math.sin(m.age*3)*.04);ctx.fillStyle='#6d4a27';ctx.strokeStyle='#e1b36e';ctx.lineWidth=2;ctx.beginPath();roundRectPath(-m.r,-m.r*.45,m.r*2,m.r*1.2,6);ctx.fill();ctx.stroke();ctx.fillStyle='#d1a25e';ctx.fillRect(-m.r,m.r*.05,m.r*2,4);ctx.fillStyle='#302015';ctx.fillRect(-4,-m.r*.1,8,12);if(m.mimicState!=='sleep'){ctx.fillStyle='#130b0b';ctx.beginPath();ctx.ellipse(0,m.r*.28,m.r*.7,m.r*.42,0,0,TAU);ctx.fill();ctx.fillStyle='#efe2cc';for(let i=-2;i<=2;i++){ctx.beginPath();ctx.moveTo(i*8-3,m.r*.02);ctx.lineTo(i*8+2,m.r*.23);ctx.lineTo(i*8+5,m.r*.02);ctx.fill();}ctx.fillStyle='#f5b45f';ctx.beginPath();ctx.arc(-8,-m.r*.18,3,0,TAU);ctx.arc(8,-m.r*.18,3,0,TAU);ctx.fill();}}
function drawLeech(m){ctx.rotate(Math.atan2(m.gateY-m.y,m.gateX-m.x)+Math.PI/2);ctx.strokeStyle='#e45f67';ctx.lineWidth=7;ctx.lineCap='round';ctx.beginPath();for(let i=0;i<6;i++){const y=-m.r+i*(m.r*2/5),x=Math.sin(m.age*8+i*.8)*4;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.stroke();ctx.fillStyle='#2a1014';ctx.beginPath();ctx.arc(0,-m.r,8,0,TAU);ctx.fill();ctx.strokeStyle='#f0a0a7';ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,-m.r,5,0,TAU);ctx.stroke();}
function drawShade(m){if(m.echoLife>0){ctx.save();ctx.translate(m.echoX-m.x,m.echoY-m.y);ctx.globalAlpha=m.echoLife*.35;ctx.fillStyle=m.color;ctx.beginPath();ctx.moveTo(0,-m.r);ctx.lineTo(m.r*.8,m.r*.7);ctx.lineTo(0,m.r*.35);ctx.lineTo(-m.r*.8,m.r*.7);ctx.closePath();ctx.fill();ctx.restore();}ctx.rotate(Math.sin(m.age*3)*.12);const g=ctx.createRadialGradient(0,-4,2,0,0,m.r*1.2);g.addColorStop(0,'rgba(237,210,255,.9)');g.addColorStop(.42,'rgba(160,96,210,.58)');g.addColorStop(1,'rgba(80,30,110,0)');ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,m.r*1.25,0,TAU);ctx.fill();ctx.fillStyle='#28152f';ctx.beginPath();ctx.moveTo(0,-m.r);ctx.quadraticCurveTo(m.r*.95,-m.r*.2,m.r*.65,m.r*.8);ctx.lineTo(0,m.r*.4);ctx.lineTo(-m.r*.65,m.r*.8);ctx.quadraticCurveTo(-m.r*.95,-m.r*.2,0,-m.r);ctx.fill();ctx.fillStyle='#f2d2ff';ctx.beginPath();ctx.arc(-5,-4,2.5,0,TAU);ctx.arc(5,-4,2.5,0,TAU);ctx.fill();}
function roundRectPath(x,y,w,h,r){ctx.beginPath();ctx.moveTo(x+r,y);ctx.arcTo(x+w,y,x+w,y+h,r);ctx.arcTo(x+w,y+h,x,y+h,r);ctx.arcTo(x,y+h,x,y,r);ctx.arcTo(x,y,x+w,y,r);ctx.closePath();}

function spawnMonster(forceType=null,elite=false){if(monsters.length>=MAX_MONSTERS)return;let pool;if(state.wave%4===1)pool=['slime','slime','bat','leech'];else if(state.wave%4===2)pool=['slime','bat','burrow','burrow','leech'];else pool=['bat','burrow','beetle','healer','mimic','leech','shade','shade'];if(currentModifier.id==='swarm')pool=pool.concat(['slime','slime','bat','bat']);if(currentModifier.id==='armor')pool=pool.concat(['beetle','beetle']);if(currentModifier.id==='plague')pool=pool.concat(['healer','healer']);const type=forceType||pool[(Math.random()*pool.length)|0];monsters.push(new Monster(type,elite));announceType(type);}
function spawnBoss(){const kind=state.wave%8===0?'weaver':'hollow';const max=(kind==='weaver'?155:140)+state.wave*28;boss={kind,x:W/2,y:H*.39,baseX:W/2,baseY:H*.39,r:Math.min(kind==='weaver'?108:120,Math.max(84,W*(kind==='weaver'?.21:.23))),hp:max,maxHp:max,phase:1,age:0,weak:0,segments:kind==='weaver'?4:6,weakTimer:0,flash:0,summonTimer:3.5,shield:0,rot:0,attackState:'idle',attackTimer:4.8,attackCharge:0,hitCd:0,dead:false};ui.bossHud.classList.remove('hidden');ui.bossName.textContent=kind==='weaver'?'渊网母体':'空洞王';ui.bossPhase.textContent=kind==='weaver'?'第一形态 · 孵化':'第一形态 · 凝视';ui.waveName.textContent=(kind==='weaver'?'渊网降临':'王座降临')+' · WAVE '+String(state.wave).padStart(2,'0');voice('boss',true);sfx('boss');}
function updateBoss(dt){if(!boss||boss.dead)return;boss.age+=dt;boss.hitCd=Math.max(0,(boss.hitCd||0)-dt);boss.rot+=dt*(.15+boss.phase*.05);boss.weakTimer-=dt;boss.summonTimer-=dt;if(boss.weakTimer<=0){boss.weak=(boss.weak+1+((Math.random()*4)|0))%boss.segments;boss.weakTimer=Math.max(1.2,2.5-boss.phase*.25);}if(boss.summonTimer<=0){boss.summonTimer=Math.max(2.2,4.6-boss.phase*.45);for(let i=0;i<1+boss.phase;i++){const type=boss.kind==='weaver'?(boss.phase>=3?'shade':(Math.random()<.5?'burrow':'bat')):(boss.phase>=3?'mimic':(Math.random()<.5?'bat':'slime'));spawnMonster(type,boss.phase>=3&&Math.random()<.25);}}if(boss.attackState==='idle'){boss.attackTimer-=dt;if(boss.attackTimer<=0){boss.attackState='charge';boss.attackCharge=Math.max(.85,1.45-boss.phase*.15);message('王座蓄力：击中发光弱点打断',1300);sfx('boss');}}else{boss.attackCharge-=dt;if(boss.attackCharge<=0){state.gate=Math.max(0,state.gate-1);state.shake=14;state.flash=.45;vibrate([18,28,18]);message('王座冲击命中夜门',1200);boss.attackState='idle';boss.attackTimer=Math.max(2.6,5.2-boss.phase*.65);if(state.gate<=0){finishRun();return;}if(state.gate<=2)voice('gate');}}const ratio=Math.max(0,boss.hp/boss.maxHp);const next=ratio<.33?3:ratio<.66?2:1;if(next!==boss.phase){boss.phase=next;boss.flash=1;sfx('boss');message('空洞王进入第 '+next+' 形态',1200);ui.bossPhase.textContent=boss.kind==='weaver'?(next===2?'第二形态 · 织网':next===3?'第三形态 · 裂巢':'第一形态 · 孵化'):(next===2?'第二形态 · 召唤':next===3?'第三形态 · 狂暴':'第一形态 · 凝视');if(next===2)boss.shield=3;if(next===3)boss.summonTimer=.6;}ui.bossFill.style.width=(ratio*100)+'%';}
function finishBoss(){if(!boss||boss._finalized)return;boss._finalized=true;monsters.length=0;boss.dead=true;state.score+=250+state.wave*20;state.skill=100;addParticles(boss.x,boss.y,colors.amber,80,2.2);addRing(boss.x,boss.y,colors.amber,boss.r*.7,5);state.flash=.8;state.shake=14;sfx('skill');voice('bossKill',true);boss=null;ui.bossHud.classList.add('hidden');const token=runToken;setTimeout(()=>{if(token===runToken&&!state.gameOver)endWave();},1200);}
function bossWeakPoint(){if(!boss)return {x:0,y:0};const a=boss.rot+boss.weak*TAU/boss.segments;return {x:boss.x+Math.cos(a)*boss.r*.86,y:boss.y+Math.sin(a)*boss.r*.86};}
function hitBoss(x,y,damage){if(!boss||boss.hitCd>0)return;boss.hitCd=.075;const a=(Math.atan2(y-boss.y,x-boss.x)-boss.rot+TAU)%TAU,seg=Math.floor((a+TAU/(boss.segments*2))/(TAU/boss.segments))%boss.segments;const wp=bossWeakPoint();const weakHit=boss.kind==='weaver'?dist(x,y,wp.x,wp.y)<30:seg===boss.weak;if(!weakHit){sfx('miss');addRing(x,y,colors.red,8,2);message('弱点在别处');return;}if(boss.shield>0){boss.shield--;message('王座护盾破损');sfx('hit',1);return;}const crit=Math.random()<player.crit;const dmg=Math.max(1,Math.round(damage*(crit?2:1)*player.bossDamage));boss.hp=Math.max(0,boss.hp-dmg);boss.flash=1;state.score+=Math.round(dmg*3*state.combo*(state.feverUntil>clock?2:1));state.chain++;state.combo=1+Math.floor(state.chain/6);state.comboTimer=player.comboGrace;state.skill=clamp(state.skill+3*player.skillGain,0,100);if(boss.attackState==='charge'){boss.attackState='idle';boss.attackTimer=Math.max(2.4,4.8-boss.phase*.5);state.score+=25;message('王座蓄力已打断',950);addRing(boss.x,boss.y,colors.cyan,boss.r*.75,4);}addText(x,y-boss.r*.2,String(dmg),crit?colors.amber:colors.ink,crit?18:13);addParticles(x,y,crit?colors.amber:colors.red,crit?26:16,crit?1.6:1.1);addRing(x,y,crit?colors.amber:colors.red,18,3);if(crit)state.hitStop=.04;sfx(crit?'crit':'boss');vibrate([10,14,10]);if(boss.hp<=0)finishBoss();}
function drawBoss(){if(!boss)return;if(boss.kind==='weaver'){drawWeaverBoss();return;}const x=boss.x,y=boss.y,r=boss.r,ratio=boss.hp/boss.maxHp;const glow=ctx.createRadialGradient(x,y,10,x,y,r*1.8);glow.addColorStop(0,'rgba(228,95,103,.23)');glow.addColorStop(.5,'rgba(145,120,232,.08)');glow.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=glow;ctx.beginPath();ctx.arc(x,y,r*1.8,0,TAU);ctx.fill();ctx.save();ctx.translate(x,y);ctx.rotate(boss.rot);for(let i=0;i<6;i++){ctx.save();ctx.rotate(