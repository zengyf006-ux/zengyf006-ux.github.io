# G5 first-pass review issues

- Paper account hook still uses memory storage instead of IndexedDB.
- Order ticket exposes only market and limit, not stopMarket and stopLimit.
- Mobile terminal stacks chart, book, ticket and trades vertically instead of task switching.
- Settings, alerts and help must not remain inert placeholders.

These issues block G5 acceptance and are assigned to the next implementation batch.
